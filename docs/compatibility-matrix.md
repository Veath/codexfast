# Compatibility Matrix

This file tracks verified `Codex.app` builds for `codexfast`.

## Fields

- `Version`: `CFBundleShortVersionString`
- `Build`: `CFBundleVersion`
- `Status`: `supported`, `unsupported`, or `investigating`
- `Features`: supported feature paths for that build
- `Verified`: date of direct verification
- `Notes`: short reason or limitation

## Matrix

| Version | Build | Status | Features | Verified | Notes |
| --- | --- | --- | --- | --- | --- |
| `26.415.40636` | `1799` | `supported` | Settings Fast, `/fast`, Add-context Speed menu, Plugins access | `2026-04-20` | Matches the current strict whitelist in `src/supported-app-versions.mts`. |
| `26.417.41555` | `1858` | `supported` | Settings Fast, `/fast`, Add-context Speed menu, Plugins access | `2026-04-21` | Verified by direct bundle inspection, regression coverage, and real-app smoke validation including the sidebar `Plugins` entry fix. |
| `26.422.21637` | `2056` | `supported` | Settings Fast, `/fast`, Intelligence Speed menu, GPT-5.5 model list, Plugins access | `2026-04-24` | Verified by direct bundle inspection and regression coverage. The composer Speed path moved to the `Intelligence` dropdown. |
| `26.422.30944` | `2080` | `supported` | Settings Fast, `/fast`, Intelligence Speed menu, Plugins access | `2026-04-25` | Verified by direct installed-bundle inspection, user validation that GPT-5.5 is already visible through the official path, and regression coverage. `codexfast` skips GPT-5.5 runtime targets from this version onward. |
| `26.422.62136` | `2176` | `supported` | Settings Fast, `/fast`, Intelligence Speed menu, Plugins access | `2026-04-28` | Verified by direct installed-bundle inspection and regression coverage. The Settings target moved to `general-settings-C7RhZXaE.js` with an `xe()` service-tier hook, and GPT-5.5 remains on the official app path. |
| `26.422.62136` | `2180` | `supported` | Settings Fast, `/fast`, Intelligence Speed menu, Plugins access | `2026-04-28` | Verified by direct installed-bundle inspection and regression coverage. The target file names and gate signatures match build `2176`; GPT-5.5 remains on the official app path. |
| `26.422.71525` | `2210` | `supported` | Settings Fast, `/fast`, Intelligence Speed menu, Plugins access | `2026-04-29` | Verified by direct installed-bundle inspection and regression coverage. The Settings target moved to `general-settings-CDNVRswg.js` with an `N()` availability gate and `ye()` service-tier hook; the Intelligence Speed gate uses `gf()` with `Yp()`, and Plugins uses `Qf("533078438")`. GPT-5.5 remains on the official app path. |
| `26.429.20946` | `2312` | `supported` | Settings Fast, `/fast`, Intelligence Speed menu, Plugins access | `2026-05-01` | Verified by direct installed-bundle inspection and regression coverage. The Settings target moved to `general-settings-BBebpJo8.js` with a `de()` availability gate and `Ve()` service-tier hook; `/fast` and Intelligence Speed moved to `composer-B5UwBne4.js`, with Intelligence Speed gated by `qs(...)`; Plugins uses `ms("533078438")`, `ed(authMethod)`, and `g({hostId:Or})` in the sidebar plus separate page-content, plugin-detail, plugin-install availability, and install-modal content gates. GPT-5.5 remains on the official app path. |
| `26.429.30905` | `2345` | `supported` | Settings Fast, `/fast`, Intelligence Speed menu, Plugins access | `2026-05-02` | Verified by direct installed-bundle inspection and regression coverage. The target mapping matches build `2312` with renamed assets: `general-settings-DK9Gn4dx.js`, `composer-CNnjHdHK.js`, `index-DJATSIwz.js`, `skills-page-DowG2k1q.js`, `plugin-detail-page-BZVYeV3B.js`, and `use-plugin-install-flow-IA7dXDS4.js`. The Intelligence Speed `qs(...)` gate now sits next to `zr(DR,n)`. GPT-5.5 remains on the official app path. |
| `26.429.61741` | `2429` | `supported` | Settings Fast, `/fast`, Intelligence Speed menu, Plugins access | `2026-05-06` | Verified by direct installed-bundle inspection and regression coverage. The target mapping matches build `2345` with the same inspected asset names and gate signatures: `general-settings-DK9Gn4dx.js`, `composer-CNnjHdHK.js`, `index-DJATSIwz.js`, `skills-page-DowG2k1q.js`, `plugin-detail-page-BZVYeV3B.js`, and `use-plugin-install-flow-IA7dXDS4.js`. GPT-5.5 remains on the official app path. |
| `26.506.21252` | `2575` | `supported` | Settings Fast, `/fast`, Intelligence Speed menu, Plugins access | `2026-05-08` | Verified by direct installed-bundle inspection and regression coverage. The Settings target moved to `general-settings-BNOywoSY.js` with an `ie()` availability gate and `de()` service-tier hook; `/fast` and Intelligence Speed are in `composer-D82P7v-B.js`, with Intelligence Speed gated by `va(...)` next to `fi(_T,n)`. Plugins sidebar access moved to `app-main-DOFYRRSd.js` using `rs("533078438")`, `Xc(authMethod)`, and `Se({hostId:eo})`; page content, detail access, install availability, and modal content moved to `skills-page-CN4AdIT4.js`, `plugin-detail-page-Dht2dFf-.js`, and `plugins-availability-DMayGLTU.js`. GPT-5.5 remains on the official app path. |
| `26.506.31421` | `2620` | `supported` | Settings Fast, `/fast`, Intelligence Speed menu, Plugins access | `2026-05-09` | Verified by direct installed-bundle inspection and regression coverage. The target mapping matches build `2575` with renamed assets: `general-settings-S-ejdEdp.js`, `composer-DawxvKsB.js`, `app-main-Bucm979x.js`, `skills-page-CrAHPBnF.js`, `plugin-detail-page-8pMyEW6A.js`, and `plugins-availability-DW4iBzVW.js`. The Plugins sidebar helper changed from `rs("533078438")` to `Qo("533078438")` and the nav-label availability helper changed to `Nc({hostId:Ya})`. GPT-5.5 remains on the official app path. |
| `26.513.20950` | `2816` | `supported` | Settings Fast, `/fast`, Intelligence Speed menu, Plugins access | `2026-05-15` | Verified by direct installed-bundle inspection, real runtime-launch validation, and regression coverage. The Settings target moved to `general-settings-Bvwhh0-i.js` with `se()` availability and `fe()` service-tier hook; `/fast` and the Intelligence Speed menu are in `composer-CL8HPtlL.js`, with Speed rendering through an inline `ve=v?...:null` assignment. Plugins sidebar access moved to `app-main-Dsg36Y4q.js` using `Is("533078438")`, `Ml(authMethod)`, and `hl({hostId:Io})`; page content, detail access, install availability, modal content, and composer plugin mentions moved to `skills-page-ydcMfNsx.js`, `plugin-detail-page-CQ7HXo6z.js`, `check-plugin-availability-Cl1_8Dsf.js`, `plugins-availability-Ch97MI4l.js`, and `prosemirror-DbRBBq50.js`. Runtime launch requests these assets as `app://-/assets/*.js`. GPT-5.5 remains on the official app path. |
| `26.513.31313` | `2867` | `supported` | Settings Fast, `/fast`, Intelligence Speed menu, Plugins access | `2026-05-17` | Verified by direct installed-bundle inspection and regression coverage. The target mapping matches build `2816` with renamed assets: `general-settings-DWGOfOIB.js`, `composer-DXaiOlFj.js`, `app-main-DZOIl7aU.js`, `skills-page-BCITaFO4.js`, `plugin-detail-page-CL4xxcbU.js`, `check-plugin-availability-BLUA-GwE.js`, `plugins-availability-DpWDTiPu.js`, and `prosemirror-PI_17HLA.js`. The composer Speed render component changed from `KR` to `qR`, Plugins sidebar helpers changed to `ec("533078438")`, `Xl(authMethod)`, and `Nl({hostId:Qo})`, and composer plugin mentions now gate `shared-with-me` behind `N("1269116100")`. GPT-5.5 remains on the official app path. |
| `26.519.22136` | `3003` | `supported` | Settings Fast, `/fast`, Intelligence Speed menu, Plugins access | `2026-05-22` | Verified by direct installed-bundle inspection and regression coverage. The Settings target moved to `general-settings-BZV2EA65.js` with `xe()` availability, `fe()` service-tier hook, and a preserved `availableOptions.length<=1` guard; `/fast` is now generated from service-tier slash commands in `composer-C6IpJrmm.js`; the Intelligence Speed target keeps the option-count guard while removing the Fast availability guard. Plugins sidebar access moved to `app-main-BIL8Sji8.js` using `Li("533078438")`, `Cc(authMethod)`, and `bs({hostId:Tt})`; page content, detail access, install availability, and modal content moved to `skills-page-HlnQvZ8j.js`, `plugin-detail-page-Rk9OBOvV.js`, `check-plugin-availability-6p9UsIaB.js`, and `use-plugin-install-flow-B_gXb-Jd.js`. GPT-5.5 remains on the official app path. |
| `26.519.31651` | `3017` | `supported` | Settings Fast, `/fast`, Intelligence Speed menu, Plugins access | `2026-05-22` | Verified by direct installed-bundle inspection and regression coverage. The target mapping matches build `3003` with renamed assets: `composer-BBBqeSy_.js` and `app-main-BsJzES7e.js`; the Settings and Plugins page/detail/install target files still match build `3003`. GPT-5.5 remains on the official app path. |
| `26.519.41501` | `3044` | `supported` | Settings Fast, `/fast`, Intelligence Speed menu, Plugins access | `2026-05-23` | Verified by direct installed-bundle inspection and regression coverage. The target mapping matches build `3003` with renamed assets: `general-settings-D_BmV12Y.js`, `composer-D0cvMZjq.js`, `app-main-DG-Mf4Wj.js`, `skills-page-C8PW4EqX.js`, `plugin-detail-page-jAJa26RM.js`, and `use-plugin-install-flow-IT_xMrDV.js`; plugin install availability still uses `check-plugin-availability-6p9UsIaB.js`. GPT-5.5 remains on the official app path. |
| `26.519.81530` | `3178` | `supported` | Settings Fast, `/fast`, Intelligence Speed menu, Plugins access | `2026-05-27` | Verified by direct installed-bundle inspection and regression coverage. The target mapping matches build `3003` with renamed assets: `general-settings-ChHT-8De.js`, `composer-EYkAbDY0.js`, `app-main-BnTSnuSB.js`, `skills-page-iJXlhk3u.js`, `plugin-detail-page-txpGRO8P.js`, and `use-plugin-install-flow-BqDhdae4.js`; plugin install availability moved to `check-plugin-availability-CKUOqQro.js`. GPT-5.5 remains on the official app path. |
| `26.527.31326` | `3390` | `supported` | Settings Fast, `/fast`, Intelligence Speed menu, Plugins access | `2026-05-30` | Verified by direct installed-bundle inspection and regression coverage. The Settings target moved to `general-settings-BqYyun_1.js` with `je()` availability, `_e()` service-tier hook, and a preserved `availableOptions.length<=1` guard; `/fast` and Intelligence Speed moved to `composer-zFOdryLS.js`; Plugins sidebar access moved to `app-main-BxvNtdQT.js` using `xa("533078438")`, `wl(authMethod)`, and `mc({hostId:mr})`; page content, detail access, install availability, and modal content moved to `skills-page-Cqn6vECJ.js`, `plugin-detail-page-CETDWYs4.js`, `check-plugin-availability-fTZpqnCL.js`, and `use-plugin-install-flow-BXFieYft.js`. GPT-5.5 remains on the official app path. |
| `26.527.60818` | `3437` | `supported` | Settings Fast, `/fast`, Intelligence Speed menu, Plugins access | `2026-06-02` | Verified by direct installed-bundle inspection and regression coverage. The target mapping matches build `3390` with renamed assets: `general-settings-Bt2lh7rT.js`, `composer-CwxGJF3C.js`, `app-main-BwpsB7rB.js`, `skills-page-BIZqKbZI.js`, `plugin-detail-page-uf22h4TJ.js`, `check-plugin-availability-CDJyCxkN.js`, and `use-plugin-install-flow-DpUQcozA.js`; Plugins sidebar helpers changed to `lc(authMethod)` and `hc({hostId:mr})`. GPT-5.5 remains on the official app path. |
| `26.601.21317` | `3511` | `supported` | Settings Fast, `/fast`, Intelligence Speed menu, Plugins catalog/install gates | `2026-06-03` | Verified by direct installed-bundle inspection and regression coverage. The Settings target moved to `general-settings-DobuGNrH.js` with a destructured `isServiceTierAllowed` gate and `availableOptions.length<=1` fallback; `/fast` and Intelligence Speed moved to `composer-CUO1FiyC.js`, with Intelligence Speed using an `_ && availableOptions.length>1` boolean gate. The old Plugins sidebar/page/detail custom-API gates are not present in the inspected bundle; plugin install availability and detail app-connect fallback are in `check-plugin-availability-C1II8bXB.js`; install modal content and post-install app connect are in `use-plugin-install-flow-C0YRtVkW.js`; curated OpenAI plugin catalog visibility is in `use-plugins-BKOuT9XR.js`; and shared plugin marketplace prefetch is in `app-prefetch-impl-CvoY4gd-.js`. GPT-5.5 remains on the official app path. |
| `26.602.30954` | `3575` | `supported` | Settings Fast, `/fast`, Intelligence Speed menu, Plugins catalog/install gates | `2026-06-05` | Verified by direct installed-bundle inspection and regression coverage. The target mapping matches build `3511` with renamed assets: `general-settings-DGJ5NwEx.js`, `composer-B7sGHJVq.js`, `check-plugin-availability-DzefvT4E.js`, `use-plugin-install-flow-Dizh5Oce.js`, `use-plugins-D190QcN7.js`, and `app-prefetch-impl-lZAOgYgS.js`. The post-install app-connect session setter changed from `F({apps:...})` to another minified local, so the apps replacement now captures that setter name. GPT-5.5 remains on the official app path. |
| `26.602.40724` | `3593` | `supported` | Settings Fast, `/fast`, Intelligence Speed menu, Plugins catalog/install gates | `2026-06-06` | Verified by direct installed-bundle inspection and regression coverage. The target mapping matches build `3575`; the inspected target files are still `general-settings-DGJ5NwEx.js`, `composer-B7sGHJVq.js`, `check-plugin-availability-DzefvT4E.js`, `use-plugin-install-flow-Dizh5Oce.js`, and `use-plugins-D190QcN7.js`, while shared plugin marketplace prefetch moved to `app-prefetch-impl-DnQBF2sE.js`. GPT-5.5 remains on the official app path. |
| `26.602.71036` | `3685` | `supported` | Settings Fast, `/fast`, Intelligence Speed menu, Plugins catalog/install gates | `2026-06-09` | Verified by direct installed-bundle inspection and regression coverage. The target mapping matches build `3593` with renamed assets: `general-settings-20gruT7U.js`, `composer-BVAB1vz9.js`, `use-service-tier-settings-CZz5xU_T.js`, `check-plugin-availability-CrPVurZq.js`, `use-plugin-install-flow-fF2AB99L.js`, `use-plugins-C_eNkElf.js`, and `app-prefetch-impl-CEVuyrmZ.js`. GPT-5.5 remains on the official app path. |
| `26.608.12217` | `3722` | `supported` | Settings Fast, `/fast`, Intelligence Speed menu, Plugins catalog/install gates | `2026-06-10` | Verified by direct installed-bundle inspection and regression coverage. The target mapping matches build `3685` with renamed assets: `general-settings-CuvKPfoV.js`, `composer-D1QtVouy.js`, `use-service-tier-settings-C421Blkq.js`, `check-plugin-availability-U7DaYfLx.js`, `use-plugin-install-flow-BgKqiNxL.js`, `use-plugins-DBCbdcYi.js`, and `app-prefetch-impl-BAy3guSD.js`. GPT-5.5 remains on the official app path. |
| `26.609.30741` | `3808` | `supported` | Settings Fast, `/fast`, Intelligence Speed menu, Plugins catalog/install gates | `2026-06-12` | Verified by direct installed-bundle inspection and regression coverage. The target mapping matches build `3722` with renamed assets: `general-settings-wTvoP-Ic.js`, `composer-D9quMRYW.js`, `use-service-tier-settings-BfIAZ7X5.js`, `check-plugin-availability-DOzacsVy.js`, `use-plugin-install-flow-vQGq_XFa.js`, `use-plugins-DbZs8ZDu.js`, and `app-prefetch-impl-CK0ThLdh.js`. GPT-5.5 remains on the official app path. |
| `26.609.41114` | `3888` | `supported` | Settings Fast, `/fast`, Intelligence Speed menu, Plugins catalog/install gates | `2026-06-13` | Verified by direct installed-bundle inspection and regression coverage. The target mapping matches build `3808` with renamed assets: `general-settings--de5RLfQ.js`, `composer-DhWyK5QW.js`, `use-service-tier-settings-DqX3ThXF.js`, `check-plugin-availability-DoPoBAuk.js`, `use-plugin-install-flow-CNyGpzoK.js`, `use-plugins-BseDp1AO.js`, and `app-prefetch-impl-BVvcvGAT.js`. GPT-5.5 remains on the official app path. |
| `26.609.71450` | `3965` | `supported` | Settings Fast, `/fast`, Intelligence Speed menu, Plugins catalog/install gates | `2026-06-16` | Verified by direct installed-bundle inspection and regression coverage. The target mapping matches build `3888` with the same inspected target files: `general-settings--de5RLfQ.js`, `composer-DhWyK5QW.js`, `use-service-tier-settings-DqX3ThXF.js`, `check-plugin-availability-DoPoBAuk.js`, `use-plugin-install-flow-CNyGpzoK.js`, `use-plugins-BseDp1AO.js`, and `app-prefetch-impl-BVvcvGAT.js`. GPT-5.5 remains on the official app path. |
| `26.611.61049` | `3996` | `supported` | Settings Fast, `/fast`, Intelligence Speed menu, Plugins catalog/install gates | `2026-06-17` | Verified by direct installed-bundle inspection and regression coverage. The target mapping matches build `3965` with renamed assets: `general-settings-BrSCFSM1.js`, `composer-DWFAaSck.js`, `use-service-tier-settings-COAzInwV.js`, `check-plugin-availability-Dt8gcp_v.js`, `use-plugin-install-flow-CNEV_P5X.js`, `use-plugins-DCH1nRSg.js`, and `app-prefetch-impl-CEgE9TWt.js`; plugin install availability and detail app-connect needed updated signatures. GPT-5.5 remains on the official app path. |
| `26.611.61753` | `4008` | `supported` | Settings Fast, `/fast`, Intelligence Speed menu, Plugins catalog/install gates | `2026-06-17` | Verified by direct installed-bundle inspection and regression coverage. The target mapping matches build `3996` with the same inspected target files: `general-settings-BrSCFSM1.js`, `composer-DWFAaSck.js`, `use-service-tier-settings-COAzInwV.js`, `check-plugin-availability-Dt8gcp_v.js`, `use-plugin-install-flow-CNEV_P5X.js`, `use-plugins-DCH1nRSg.js`, and `app-prefetch-impl-CEgE9TWt.js`. GPT-5.5 remains on the official app path. |
| `26.611.62324` | `4028` | `supported` | Settings Fast, `/fast`, Intelligence Speed menu, Plugins catalog/install gates | `2026-06-18` | Verified by direct installed-bundle inspection and regression coverage. The target mapping matches build `4008` with renamed assets: `general-settings-IMuz9tUl.js`, `composer-_Sq5SNWs.js`, `use-service-tier-settings-C5mC2768.js`, `check-plugin-availability-Ddzzxdl0.js`, `use-plugin-install-flow-4i8rVI1e.js`, `use-plugins-D38FFcwl.js`, and `app-prefetch-impl-CORPetQR.js`. GPT-5.5 remains on the official app path. |
| `26.616.31447` | `4133` | `supported` | Settings Fast, `/fast`, Intelligence Speed menu, Plugins catalog/install gates | `2026-06-19` | Verified by direct installed-bundle inspection and regression coverage. The target family matches build `4028` with renamed assets: `general-settings-w5rHBc1G.js`, `composer-DPGDuJAT.js`, `use-service-tier-settings-DdUlOJCi.js`, `check-plugin-availability-B7Jyfq8B.js`, `use-plugin-install-flow-SRPoE-G1.js`, `use-plugins-h49NfL4I.js`, and `app-prefetch-impl-DN5qPrHb.js`. The composer Speed gate needed a code-level `composer.openModelPicker` needle because the locale message id moved out of the composer asset. The Plugins catalog query also needs the local full plugin cache root to surface app-connect plugins such as Gmail when the default API catalog returns only `openai-api-curated`. GPT-5.5 remains on the official app path. |
| `26.616.51431` | `4212` | `supported` | Settings Fast, `/fast`, Intelligence Speed menu, Plugins catalog/install gates | `2026-06-22` | Verified by direct installed-bundle inspection and regression coverage. The target mapping matches build `4133` with renamed assets: `general-settings-Bit-KX17.js`, `composer-CCuv6v-2.js`, `use-service-tier-settings-B3OdnJtr.js`, `check-plugin-availability-C-NsZk73.js`, `use-plugin-install-flow-C4PrQmDj.js`, `use-plugins-DBJZMsOL.js`, and `app-prefetch-impl-C8D_N7IT.js`. GPT-5.5 remains on the official app path. |
| `26.616.71553` | `4265` | `supported` | Settings Fast, `/fast`, Intelligence Speed menu, Plugins catalog/install gates | `2026-06-23` | Verified by direct installed-bundle inspection and regression coverage. The target mapping matches build `4212` with renamed assets: `general-settings-U7DFIZBC.js`, `composer-DlMDPaCL.js`, `use-service-tier-settings-DDtqCMIO.js`, `check-plugin-availability-DSvDEMVw.js`, `use-plugin-install-flow-DDuwrr2c.js`, `use-plugins-Dex8E4w_.js`, and `app-prefetch-impl-CtdRcCKu.js`. The post-install app-connect close and auto-open guards needed an updated callback-shape signature. GPT-5.5 remains on the official app path. |
| `26.616.81150` | `4306` | `supported` | Settings Fast, `/fast`, Intelligence Speed menu, Plugins catalog/install gates | `2026-06-24` | Verified by direct installed-bundle inspection and regression coverage. The target mapping matches build `4265`; shared plugin marketplace prefetch moved to `app-prefetch-impl-DEaoqJMj.js`, while the other inspected target files still match build `4265`. The automatic-update Settings row now uses locale-aware copy. GPT-5.5 remains on the official app path. |
| `26.623.31443` | `4441` | `supported` | Settings Fast, `/fast`, Intelligence Speed menu, official Plugins support | `2026-06-26` | Verified by direct installed-bundle inspection and regression coverage. Fast and automatic-update targets still match with renamed assets, while Plugins is supported by the official app path; codexfast skips Plugins runtime targets for this build. GPT-5.5 remains on the official app path. |
| `26.623.31921` | `4452` | `supported` | Settings Fast, `/fast`, Intelligence Speed menu, official Plugins support | `2026-06-26` | Verified by direct installed-bundle inspection and regression coverage. The `26.623.31443` target family still applies with renamed assets; the automatic-update Settings row needed a helper-name-tolerant matcher. Plugins remains supported by the official app path, so codexfast skips Plugins runtime targets for this build. GPT-5.5 remains on the official app path. |
| `26.623.42026` | `4514` | `supported` | Settings Fast, `/fast`, Intelligence Speed menu, official Plugins support | `2026-06-28` | Verified by direct installed-bundle inspection and regression coverage. The `26.623.31921` Fast target family still applies with renamed assets; the automatic-update Settings row needed a local-name-collision matcher. Plugins remains supported by the official app path, so codexfast skips Plugins runtime targets for this build. GPT-5.5 remains on the official app path. |
| `26.623.61825` | `4548` | `supported` | Settings Fast, `/fast`, Intelligence Speed menu, official Plugins support | `2026-06-29` | Verified by direct installed-bundle inspection and regression coverage. The `26.623.42026` Fast target family still applies with renamed assets; the automatic-update Settings row needed a renamed-locals matcher. Plugins remains supported by the official app path, so codexfast skips Plugins runtime targets for this build. GPT-5.5 remains on the official app path. |
| `26.623.70822` | `4559` | `supported` | Settings Fast, `/fast`, Intelligence Speed menu, official Plugins support | `2026-06-30` | Verified by direct installed-bundle inspection and regression coverage. The `26.623.61825` target family still applies with renamed assets; the automatic-update Settings row continues to match the renamed-locals matcher. Plugins remains supported by the official app path, so codexfast skips Plugins runtime targets for this build. GPT-5.5 remains on the official app path. |
| `26.623.81905` | `4598` | `supported` | Settings Fast, `/fast`, Intelligence Speed menu, official Plugins support | `2026-07-01` | Verified by direct installed-bundle inspection and regression coverage. The `26.623.70822` Fast, service-tier, `/fast`, Intelligence Speed, GPT-5.5 model-list, and automatic-update schema targets still apply with renamed assets; the automatic-update Settings row needed a hook-shape matcher. Plugins remains supported by the official app path, so codexfast skips Plugins runtime targets for this build. |
| `26.623.101652` | `4674` | `supported` | Settings Fast, `/fast`, Intelligence Speed menu, official Plugins support | `2026-07-03` | Verified by direct installed-bundle inspection and regression coverage. The `26.623.81905` Fast, service-tier, `/fast`, Intelligence Speed, GPT-5.5 model-list, automatic-update schema, hook-based Settings row, and launcher Sparkle-hook targets still apply with renamed assets. Plugins remains supported by the official app path, so codexfast skips Plugins runtime targets for this build. |
| `26.623.141536` | `4753` | `supported` | Settings Fast, `/fast`, Intelligence Speed menu, official Plugins support | `2026-07-07` | Verified by direct installed-bundle inspection and regression coverage. The `26.623.101652` Fast, service-tier, `/fast`, Intelligence Speed, GPT-5.5 model-list, automatic-update schema, Settings row, and launcher Sparkle-hook targets still apply with renamed assets. Plugins remains supported by the official app path, so codexfast skips Plugins runtime targets for this build. |
| `26.707.31428` | `5059` | `supported` | Settings Fast, `/fast`, Intelligence Speed menu, GPT-5.6 Sol/Terra/Luna model list, official Plugins support | `2026-07-10` | Verified by direct installed-bundle inspection, live renderer/query-cache diagnosis, and regression coverage against the local `/Applications/ChatGPT.app` bundle id `com.openai.codex`. The build needs new request-helper, conversation-fallback, app-name-aware Settings row, priority/source/timeout model-list, and downstream `use_hidden_models` selector signatures; the GPT-5.6 bridge exposes Max/Ultra for Sol and Terra and Max without Ultra for Luna. codexfast skips Plugins runtime targets because Plugins remains supported by the official app path. |

## Update Rules

- Add a row only after direct bundle inspection and regression updates.
- If a build is not whitelisted yet, mark it `investigating` or `unsupported`.
- When support status changes, update both this file and the whitelist in `src/supported-app-versions.mts`.
