# ChatGPT.app 26.715.61943 build 5628

## Bundle metadata

- Installed app: `/Applications/ChatGPT.app`
- Bundle identifier: `com.openai.codex`
- `CFBundleShortVersionString`: `26.715.61943`
- `CFBundleVersion`: `5628`
- Strict compatibility key: `26.715.61943+5628`
- `app.asar` SHA-256 before validation: `7501dd25c22e090bb131fe3fe6423e5c3b21b7f275c7e45b86ebe00a68052c80`
- `Info.plist` SHA-256 before validation: `f1ccf0c0274dcff789410a771f7d3a877df97bf9bf7aae52023ad80c301c9c9b`
- Both hashes were unchanged after validation, and `codesign --verify --deep --strict` passed.

## Renderer findings

- Settings Fast and the automatic-update row: `webview/assets/general-settings-BWZCvLqI.js`
- Shared Fast allowance and configured-tier fallback: the `l46phxln` shared chunk
- Request-tier allowance: the `b0wqjrxp` shared chunk
- `/fast` and Intelligence Speed: the `unq8yzli` shared chunk
- Automatic-update Settings target: existing `disable-automatic-updates-setting-26715-row-local-h`
- GPT-5.6: official application path; compatibility injection and selector widening are skipped
- Plugins: official application path; Plugins runtime targets and the legacy required initial label are skipped

Applying the version-filtered renderer patch rules to all 4,650 extracted
JavaScript files changed five renderer modules: the shared Fast allowance/fallback
chunk, the Intelligence Speed and slash-command chunk, the request-tier chunk,
the Settings chunk, and one module carrying the desktop settings schema copy.
All changed JavaScript parsed successfully after patching. Main-process source
signature interception changed the three settings-schema copies and
`window-all-closed-CZr9g6FK.js`; manual update methods remain intact.

## Validation boundary

- Direct installed-bundle inspection, version-filtered extracted-bundle patching,
  JavaScript parse checks, and regression coverage were performed.
- The installed `ChatGPT.app` signature was valid before and after validation,
  and no installed app files were written.
- ChatGPT was running during validation, so it was not forcibly quit and a fresh
  interactive runtime-launch smoke pass was not performed. Full launch and UI
  click-through remain separate manual checks from
  [`../real-app-validation.md`](../real-app-validation.md).
