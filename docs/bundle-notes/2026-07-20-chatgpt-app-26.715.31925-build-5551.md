# ChatGPT.app 26.715.31925 build 5551

## Bundle metadata

- Installed app: `/Applications/ChatGPT.app`
- Bundle identifier: `com.openai.codex`
- `CFBundleShortVersionString`: `26.715.31925`
- `CFBundleVersion`: `5551`
- Strict compatibility key: `26.715.31925+5551`
- Extracted bundle used for validation: `/tmp/codexfast-5551.scCnZ4/app`
- `app.asar` SHA-256: `0c9dd677134340cb944e7642b8bc2504c7b73c7dc334d9d756547858171eea41`
- `Info.plist` SHA-256: `d251f26282d263fb8593a8b6dd25e195ab1c6e1955a4d2542b08f48bbf4b688e`

## Renderer findings

- Settings Fast and the automatic-update row: `webview/assets/general-settings-Boi5S8Wz.js`
- Shared Fast allowance and configured-tier fallback: the `l46phxln` shared chunk
- Request-tier allowance: the `b0wqjrxp` shared chunk
- `/fast` and Intelligence Speed: the `unq8yzli` shared chunk
- Automatic-update Settings target: existing `disable-automatic-updates-setting-26715-row-local-h`
- GPT-5.6: official application path; compatibility injection and selector widening are skipped
- Plugins: official application path; Plugins runtime targets and the legacy required initial label are skipped

Applying the generated version-filtered renderer patcher to all 4,981 extracted
JavaScript files patched the complete Fast and automatic-update set with zero
JavaScript parse diagnostics. No extracted files were written.

## Main-process updater findings

- The desktop settings schema continues to match the stable `preventSleepWhileRunning` source signature.
- The active Sparkle updater is `.vite/build/window-all-closed-CZr9g6FK.js`.
- The existing callback-aware source-signature hook inserts the dynamic config check, preserves the production-appcast condition, and keeps manual `checkForUpdates` and `installUpdatesIfAvailable` methods intact.
- Applying the settings and updater helpers across 20 `.vite/build/*.js` files changed four modules (three schema copies and the updater), with zero JavaScript parse diagnostics.

## Validation boundary

- Direct bundle inspection, regression coverage, generated CLI checks, and installed-app hash/signature checks were performed.
- No installed `ChatGPT.app` files were modified.
- A fresh interactive `codexfast launch` smoke pass is separate from this extracted-bundle proof; UI click-through still requires the normal manual checklist in `docs/real-app-validation.md`.
