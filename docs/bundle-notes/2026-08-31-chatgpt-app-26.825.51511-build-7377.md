# ChatGPT.app 26.825.51511 (build 7377)

## Identification

- Bundle: `/Applications/ChatGPT.app`
- Bundle identifier: `com.openai.codex`
- `CFBundleShortVersionString`: `26.825.51511`
- `CFBundleVersion`: `7377`
- Inspected: `2026-08-31`

## Findings

- Renderer assets moved to `app-initial-B6Gk5KCN.js` and `general-settings-D25XHuE_.js`. Existing Fast allowance, request-tier helper, configured-tier fallback, `/fast`, settings schema, and Settings Fast targets still match.
- Main-process settings schema copies are in `.vite/build/src-C2ILPx7g.js` and `.vite/build/worker.js`. Source-signature updater discovery matches `.vite/build/window-all-closed-DCMYPAWO.js`.
- GPT-5.6 and Plugins remain official in this build. Runtime launch skips the GPT-5.6 injection/query-selector and Plugins target families for the exact `26.825.51511+7377` key.

## Validation

- Extracted `app.asar` read-only with `@electron/asar` into a temporary directory.
- Inspected 7,013 JavaScript files and applied the current patch engine to the extracted tree. The expected renderer and main-process targets matched; official GPT-5.6 and Plugins paths remain skipped by version filtering.
- The installed app bundle was not modified during extraction or validation. A fresh runtime launch and visible Settings/Plugins smoke check were not run because ChatGPT was already running.
