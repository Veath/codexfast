# Codex.app 26.422.62136 Build 2176

## Summary

This note records the inspected bundle shape for the newly supported `Codex.app` build.

- Version: `26.422.62136`
- Build: `2176`
- Verification date: `2026-04-28`
- Result: `supported`

## Verified Feature Paths

- Composer `/fast` slash command
- Settings-side Fast control
- Composer `Intelligence` dropdown Speed submenu
- Plugins sidebar access for custom API users
- Official GPT-5.5 model-list visibility is expected through the app path used by `26.422.30944` and later builds

## Bundle Notes

- Assets are still under `app/webview/assets` inside `app.asar`.
- The primary app target file changed to `index-BSTEB47c.js`.
- The Settings-side Fast target file changed to `general-settings-C7RhZXaE.js`.
- The Fast slash command and Plugins sidebar gate still match the existing 26.422 target shapes.
- The composer `Intelligence` Speed gate moved from the older `Jp(...),g=_f()` service-tier hook shape to `Zp(...),g=_f()`.
- The Settings-side Fast gate still uses `P()` for availability, but its service-tier hook changed from the older `be()` shape to `xe()`.
- The model-list bridge still delegates to `model/list`, but `codexfast` skips GPT-5.5 apply targets on this build because GPT-5.5 is expected to be visible through the official app path from `26.422.30944` onward.

## Gate Notes

- The Settings-side Fast control still uses the guarded `if(!n)return null;` shape.
- The Fast slash command still uses `enabled:n`, where `n` is derived from `_f()`.
- The composer `Intelligence` Speed menu still gates the submenu with a local `_f()` assignment.
- The Plugins sidebar disabled-nav state still derives from `$f("533078438")`, `authMethod==="apikey"`, and the adjacent `O&&k` local.
- The unified Skills/Plugins nav label state still derives from `Ha({hostId:me})&&!k`.

## Verification Notes

- Real installed-app status detection initially reported the build as unsupported and found only the `/fast` and Plugins targets before the new signature updates.
- A follow-up user validation found the Settings Speed control was still hidden; direct bundle inspection then identified the `general-settings-C7RhZXaE.js` / `xe()` Settings target.
- Direct bundle inspection confirmed the new `Zp(...),g=_f()` Intelligence Speed signature.
- Shell regression coverage now includes the `26.422.62136` (`build 2176`) whitelist gate and apply/restore behavior for the new Settings and Intelligence Speed target shapes.
