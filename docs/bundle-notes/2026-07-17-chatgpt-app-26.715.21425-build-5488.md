# ChatGPT.app 26.715.21425 build 5488

## Bundle metadata

- Installed app: `/Applications/ChatGPT.app`
- Bundle identifier: `com.openai.codex`
- `CFBundleShortVersionString`: `26.715.21425`
- `CFBundleVersion`: `5488`
- Strict compatibility key: `26.715.21425+5488`
- Extracted bundle used for validation: `/tmp/codexfast-5488.emdeSu/app`
- `app.asar` SHA-256: `5db4c67090c0521fa717e83e46cb0a6175cb6c16fb89064223753bdf05cff0aa`
- `Info.plist` SHA-256: `70be56cb90908278d5b1e996ef4a1b953b52be0cebfd6d9b7c4293ee3b24547f`

## Renderer findings

- Settings Fast: `webview/assets/general-settings-B8bUS3xL.js`
- Shared Fast allowance and configured-tier fallback: the `l46phxln` shared chunk
- Request-tier allowance: the `b0wqjrxp` shared chunk
- `/fast` and Intelligence Speed: the `unq8yzli` shared chunk
- Automatic-update Settings row: `general-settings-B8bUS3xL.js`, using `disable-automatic-updates-setting-26715-row-local-h`
- GPT-5.6: official application path; compatibility injection and selector widening are skipped
- Plugins: official application path; Plugins runtime targets and the legacy required initial label are skipped

The configured Settings service tier remains authoritative for existing conversations. Applying the generated build-5488 renderer patcher to the extracted bundle must produce all Fast and automatic-update labels with zero JavaScript parse diagnostics.

## Main-process updater findings

- The desktop settings schema continues to match the stable `preventSleepWhileRunning` source signature.
- The active Sparkle manager moved to `.vite/build/window-all-closed-DXvqe7lL.js`.
- `initializeMacSparkle()` defines a local background callback and schedules it only outside the production-appcast branch.
- `setAutomaticBackgroundDownloadsEnabledForMac` reuses the same callback behind that condition.
- The callback-aware hook preserves the production-appcast condition, routes scheduled and automatic-download-triggered checks through `codexfastAutomaticUpdateCheck`, and continues to guard `scheduleForcedUpdateInstall()`.
- Manual `checkForUpdates` and `installUpdatesIfAvailable` methods remain unchanged.

## Validation boundary

- ChatGPT was running throughout the adaptation and was not terminated.
- A real `node bin/codexfast launch` pass was not run because launch requires the app to be fully quit.
- Regression coverage includes the strict gate, official Plugins/GPT-5.6 filtering, Settings syntax and idempotency, callback-aware updater behavior, generated-hook parity, generated CLI behavior, and extracted-bundle parsing.
- The installed `app.asar`, `Info.plist`, and application signature must remain unchanged after validation.
