# ChatGPT.app 26.818.61809 (build 7019)

## Identification

- Bundle: `/Applications/ChatGPT.app`
- Bundle identifier: `com.openai.codex`
- `CFBundleShortVersionString`: `26.818.61809`
- `CFBundleVersion`: `7019`
- Inspected: `2026-08-25`

## Findings

- Renderer assets moved to `app-initial-q5My48Y-.js` and `general-settings-D4Zn65Pe.js`. Existing Fast service-tier allowance, request helper, configured-tier fallback, `/fast`, Plugins, Settings Fast, and automatic-update row targets still match.
- Main-process settings schema copies are in `.vite/build/src-DlBR1tzg.js` and `.vite/build/worker.js`. The active updater is `.vite/build/window-all-closed-BgGkNcoi.js`; the existing source-signature discovery hook matches its nested callback/interval shape without depending on the chunk basename.
- GPT-5.6 and Plugins remain official in this build. Runtime launch skips the GPT-5.6 injection/query-selector and Plugins target families for the exact `26.818.61809+7019` key.

## Validation

- Extracted `app.asar` read-only with `@electron/asar` into a temporary directory.
- Existing runtime target inspection found the expected guarded Fast, update, and legacy Plugins compatibility signatures. The bundle contains two Composer mention-list marketplace gates; the Composer Plugins target now replaces both in one pass.
- Applied the runtime patcher to the extracted renderer and main-process sources; patched JavaScript parsed successfully and repeated patching remained idempotent.
- The installed app bundle was not modified or relaunched.
