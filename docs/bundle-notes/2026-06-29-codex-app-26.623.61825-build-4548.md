# Codex.app 26.623.61825 Build 4548

## Summary

This note records the inspected bundle shape for the newly supported `Codex.app` build.

- Version: `26.623.61825`
- Build: `4548`
- Verification date: `2026-06-29`
- Result: `supported`

## Verified Feature Paths

- Composer `/fast` slash command
- Settings-side Fast control
- Composer `Intelligence` dropdown Speed submenu
- Settings > General `Disable automatic updates` row, plus dynamic background Sparkle check suppression through the launcher main-process hook
- Official Plugins support through the app path, with codexfast Plugins runtime targets skipped for this build
- Official GPT-5.5 model-list visibility is expected through the app path used by `26.422.30944` and later builds

## Bundle Notes

- Assets are still under `app/webview/assets` inside `app.asar`.
- Settings-side Fast moved to `general-settings-CUoq9KS4.js` and still uses the destructured `isServiceTierAllowed` gate with `serviceTierSettings.availableOptions.length<=1`.
- The shared service-tier allowance and conversation fallback targets moved to `app-initial~app-main~worktree-init-v2-page~remote-conversation-page~onboarding-page~hotkey-~ke3yc5wu-CfZV4-S3.js`.
- The request service-tier allowance target moved to `app-initial~app-main~worktree-init-v2-page~remote-conversation-page~new-thread-panel-page~o~hgx54pg3-D4ItPAoC.js`.
- The `/fast` slash command and composer `Intelligence` Speed menu target moved to `app-initial~app-main~remote-conversation-page~new-thread-panel-page~appgen-library-page~hot~djo67r4n-D92FAoMo.js`.
- The automatic-update Settings row still uses the `disable-automatic-updates-setting-26623` family in `general-settings-CUoq9KS4.js`, but the minified local variables changed again to a `let s,c` and `let l=o??!1,u` layout, so `disable-automatic-updates-setting-26623-renamed-locals` handles this build. The replacement must use a codexfast-prefixed Intl local; generating `t=E(i),...,i=H()` shadows the bundle's settings atom module local and crashes the lazy Settings chunk through temporal-dead-zone access.
- The automatic-update settings schema target matches `.vite/build/child-process-snapshot-worker.js`.
- The launcher-side Sparkle hook target moved to `.vite/build/workspace-root-drop-handler-DeLi4ACJ.js`; the inspected background check shape remains `let f=XB();f>0&&setInterval(d,f).unref(),d()`, and the automatic background download gate still calls through `setAutomaticBackgroundDownloadsEnabledForMac=e=>{c.setAutomaticBackgroundDownloadsEnabled(e),e&&d()}`.
- The old Plugins sidebar disabled-nav target, `/skills` page-content auth block, and plugin detail redirect target were not present in the inspected bundle.
- Several older Plugins catalog/install/app-connect signatures are still detectable by the generic target inspector, but this build supports Plugins through the official app path. Runtime launch filters out all Plugins target ids for `26.623.61825+4548`.

## Verification Notes

- `Info.plist` reported `CFBundleShortVersionString` `26.623.61825` and `CFBundleVersion` `4548` from `/Applications/Codex.app/Contents/Info.plist`.
- Before the compatibility update, `node bin/codexfast launch` reported `Compatibility: unsupported` for `26.623.61825+4548`.
- Direct bundle inspection reported guarded matches for Settings Fast, service-tier allowance, service-tier request allowance, service-tier conversation fallback, service-tier slash command, composer `Intelligence` Speed through the code-level target, the automatic-update schema target, and the `disable-automatic-updates-setting-26623-renamed-locals` General settings target.
- Direct bundle inspection did not find the old Plugins sidebar/page/detail API-key gates.
- Regression coverage includes the `26.623.61825` (`build 4548`) whitelist gate, the no-legacy-Plugins-required runtime launch rule, the runtime patcher wrapper that skips Plugins target ids while preserving non-Plugins targets for this build, and the renamed-locals Settings shape for the automatic-update row, including a guard against `t=E(i),...,i=H()` local shadowing.
