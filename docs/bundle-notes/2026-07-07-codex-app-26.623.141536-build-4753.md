# Codex.app 26.623.141536 Build 4753

## Summary

This note records the inspected bundle shape for the newly supported `Codex.app` build.

- Version: `26.623.141536`
- Build: `4753`
- Verification date: `2026-07-07`
- Result: `supported`

## Verified Feature Paths

- Composer `/fast` slash command
- Settings-side Fast control
- Composer `Intelligence` dropdown Speed submenu
- Settings > General `Disable automatic updates` row, plus dynamic background Sparkle check and forced automatic install suppression through the launcher main-process hook
- Official Plugins support through the app path, with codexfast Plugins runtime targets skipped for this build
- GPT-5.5 model-list Fast metadata augmentation

## Bundle Notes

- Assets are still under `app/webview/assets` inside `app.asar`.
- Settings-side Fast moved to `general-settings-ClkPNPB2.js` and still uses the destructured `isServiceTierAllowed` gate with `serviceTierSettings.availableOptions.length<=1`.
- The shared service-tier allowance, service-tier request allowance, and conversation fallback targets moved to `app-initial~app-main~onboarding-page-CNHnOMz8.js`.
- The `/fast` slash command and composer `Intelligence` Speed menu target moved to `app-initial~app-main~remote-conversation-page~new-thread-panel-page~appgen-library-page~hot~djo67r4n-DfdeaLvo.js`.
- The model-list handler moved to `app-initial~app-main~automations-page-BcHjEK7e.js` and still exposes the simple `"list-models-for-host"` wrapper shape that `codexfast` wraps to augment GPT-5.5 Fast metadata.
- The automatic-update Settings row matches the renamed-locals 26.623 row target and uses the hook-safe replacement with codexfast-prefixed locals.
- The automatic-update settings schema target matches `.vite/build/child-process-snapshot-worker.js`.
- The launcher-side Sparkle hook target remains `.vite/build/workspace-root-drop-handler-4fzIumU3.js`.
- The old Plugins sidebar disabled-nav target, `/skills` page-content auth block, and plugin detail redirect target were not present in the inspected bundle.
- Several older Plugins catalog/install/app-connect signatures are still detectable by the generic target inspector, but this build supports Plugins through the official app path. Runtime launch filters out all Plugins target ids for `26.623.141536+4753`.

## Verification Notes

- `Info.plist` reported `CFBundleShortVersionString` `26.623.141536` and `CFBundleVersion` `4753` from `/Applications/Codex.app/Contents/Info.plist`.
- Direct bundle inspection reported guarded matches for Settings Fast, service-tier allowance, service-tier request allowance, service-tier conversation fallback, service-tier slash command, composer `Intelligence` Speed through the code-level target, the automatic-update schema target, the renamed-locals automatic-update General settings row target, and the simple GPT-5.5 model-list handler.
- Regression coverage includes the `26.623.141536` (`build 4753`) whitelist gate, the no-legacy-Plugins-required runtime launch rule, and the runtime patcher wrapper that skips Plugins target ids while preserving non-Plugins targets for this build.
