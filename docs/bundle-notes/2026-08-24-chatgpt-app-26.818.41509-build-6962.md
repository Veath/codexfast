# ChatGPT.app 26.818.41509 (build 6962)

## Identification

- Bundle: `/Applications/ChatGPT.app`
- Bundle identifier: `com.openai.codex`
- `CFBundleShortVersionString`: `26.818.41509`
- `CFBundleVersion`: `6962`
- Inspected: `2026-08-24`

## Findings

- The renderer asset names moved to `app-initial-DwVrCWuo.js` and `general-settings-DqMQqZ_N.js`.
- Existing Fast targets still match: shared allowance, the three-argument request allowance helper, configured-tier conversation fallback, `/fast`, and the Intelligence Speed path. The Settings Fast target and dedicated `Disable automatic updates` row target also match.
- Main-process desktop settings schema copies are in `src-CLzQUgbV.js` and `worker.js`. The updater is in `window-all-closed-CSZ7EgG_.js`; source-signature discovery remains applicable, so no chunk basename was added to runtime code.
- GPT-5.6 and Plugins remain official in this build. Runtime launch skips the GPT-5.6 injection/query-selector and Plugins target families for the exact `26.818.41509+6962` key.

## Validation

- Extracted `app.asar` read-only with `@electron/asar` into a temporary directory.
- Existing runtime target inspection found the expected guarded Fast, update, and legacy Plugins compatibility signatures.
- Applied the runtime patcher to the extracted renderer assets; patched JavaScript parsed successfully and repeated patching was idempotent.
- The installed app bundle was not modified or relaunched.
