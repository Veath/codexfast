# ChatGPT.app 26.814.41407 (build 6720)

- Strict compatibility key: `26.814.41407+6720`.
- Local bundle: `/Applications/ChatGPT.app`, bundle id `com.openai.codex`, executable `ChatGPT`.
- A recursive byte-for-byte comparison of the read-only extracted `app.asar` contents against build `6662` found no differences.
- It therefore reuses `webview/assets/app-initial-BCLYDefw.js`, `webview/assets/general-settings-yzMrcfmm.js`, the main-process schema copies in `.vite/build/child-process-snapshot-worker.js`, `.vite/build/src-DY9Aq019.js`, and `.vite/build/worker.js`, and the updater in `.vite/build/window-all-closed-DqXEFZQJ.js`.
- The build reuses the three-argument Fast request-helper target added for build `6662`; no patch regex was changed or widened.
- GPT-5.6 and Plugins use official application paths. Runtime launch skips their compatibility target families for this exact version/build.
- The renderer has no `disableAutomaticUpdates` string or Settings row. The main-process schema and runtime updater hook remain compatible.
- Validation is read-only against a temporary extraction of `app.asar`; the installed bundle, `Info.plist`, signature, and running application process were not modified.
