# Codex.app 26.623.31921 Build 4452

## Summary

This note records the inspected bundle shape for the newly supported `Codex.app` build.

- Version: `26.623.31921`
- Build: `4452`
- Verification date: `2026-06-26`
- Result: `supported`

## Verified Feature Paths

- Composer `/fast` slash command
- Settings-side Fast control
- Composer `Intelligence` dropdown Speed submenu
- Settings > General `Disable automatic updates` row, plus dynamic background Sparkle check suppression through the launcher main-process hook
- Official Plugins support through the app path, with codexfast Plugins runtime targets skipped for this build
- Official GPT-5.5 model-list visibility is expected through the app path used by `26.422.30944` and later builds

## Bundle Notes

- Assets are still under `app/webview/assets` inside `app.asar`.
- Settings-side Fast moved to `general-settings-CONLsqSe.js` and still uses the destructured `isServiceTierAllowed` gate with `serviceTierSettings.availableOptions.length<=1`.
- The shared service-tier allowance target moved to `app-initial~app-main~home-ambient-suggestions-content-CGzLiN5i.js`.
- The shared service-tier conversation fallback target moved to `app-initial~app-main~onboarding-page~profile-QLPeiknY.js`.
- The request service-tier helper moved to `app-initial~app-main~remote-conversation-page~new-thread-panel-page~projects-index-page~app~ovcriy74-KTK3czaX.js` and still uses the `Failed to read service tier for request` helper path covered by `speed-service-tier-request-allowance-26623`.
- The `/fast` slash command and composer `Intelligence` Speed menu target moved to `app-initial~app-main~remote-conversation-page~new-thread-panel-page~appgen-library-page~hot~djo67r4n-CIs8dplf.js`.
- The automatic-update Settings row still uses the `disable-automatic-updates-setting-26623` target in `general-settings-CONLsqSe.js`, but the minified settings helpers changed from the `26.623.31443` names. The matcher now captures the helper, namespace, row, and toggle names instead of hard-coding them.
- The automatic-update settings schema target matches `.vite/build/child-process-snapshot-worker.js`.
- The launcher-side Sparkle hook target remains in `.vite/build/workspace-root-drop-handler-Cejfos9U.js`; the inspected background check shape is `let f=XB();f>0&&setInterval(d,f).unref(),d()`.
- The old Plugins sidebar disabled-nav target, `/skills` page-content auth block, and plugin detail redirect target were not present in the inspected bundle.
- Several older Plugins catalog/install/app-connect signatures are still detectable by the generic target inspector, but this build supports Plugins through the official app path. Runtime launch filters out all Plugins target ids for `26.623.31921+4452`.

## Verification Notes

- `Info.plist` reported `CFBundleShortVersionString` `26.623.31921` and `CFBundleVersion` `4452` from `/Applications/Codex.app/Contents/Info.plist`.
- Before the compatibility update, `node bin/codexfast launch` reported `Compatibility: unsupported` for `26.623.31921+4452`.
- Direct bundle inspection reported guarded matches for Settings Fast, service-tier allowance, service-tier request allowance, service-tier conversation fallback, service-tier slash command, composer `Intelligence` Speed through the code-level target, the automatic-update schema target, and the `disable-automatic-updates-setting-26623` General settings target.
- Direct bundle inspection did not find the old Plugins sidebar/page/detail API-key gates.
- Regression coverage includes the `26.623.31921` (`build 4452`) whitelist gate, the no-legacy-Plugins-required runtime launch rule, the runtime patcher wrapper that skips Plugins target ids while preserving non-Plugins targets for this build, and the renamed Settings-helper shape for the automatic-update row.
