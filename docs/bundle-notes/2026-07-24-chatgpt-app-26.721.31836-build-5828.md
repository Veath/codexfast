# ChatGPT.app 26.721.31836 build 5828

## Bundle metadata

- Installed app: `/Applications/ChatGPT.app`
- Bundle identifier: `com.openai.codex`
- `CFBundleShortVersionString`: `26.721.31836`
- `CFBundleVersion`: `5828`
- Strict compatibility key: `26.721.31836+5828`
- `app.asar` SHA-256 before validation: `674dab67fe39f9912493f640c1dd80f222f6062ad0f50b182a6cc87eebd0d3dc`
- `Info.plist` SHA-256 before validation: `f7805c553f1acd482a8ceb95bb60449e15b7cd8e8064de027ed067a84eedd316`
- Both hashes were unchanged after validation, and `codesign --verify --deep --strict` passed before and after inspection.

## Renderer findings

- Settings Fast and the automatic-update row: `webview/assets/general-settings-DaCT8Zmh.js`
- Shared Fast allowance, request-tier allowance, configured-tier fallback, `/fast`, and Intelligence Speed: `webview/assets/app-initial-C-fROkKo.js`
- Automatic-update Settings target: existing `disable-automatic-updates-setting-26707-app-name`
- Desktop-settings schema copies: `.vite/build/child-process-snapshot-worker.js`, `.vite/build/src-DChWimf7.js`, and `.vite/build/worker.js`
- Callback-aware Sparkle updater: `.vite/build/window-all-closed-Bz3ZcBls.js`
- GPT-5.6: official application path; compatibility model-list injection and query-selector widening are skipped
- Plugins: official application path; Plugins runtime targets and the legacy required initial label are skipped

Applying the build-5828 version-filtered patcher in memory across the extracted
`webview/assets` and `.vite/build` JavaScript changed five modules: the two
renderer assets and the three main-process settings-schema copies listed
above. Every changed module parsed successfully with the TypeScript JavaScript
parser. The exact patched-label set was:

- `Speed setting`
- `Speed service tier allowance`
- `Speed service tier request allowance`
- `Speed service tier conversation fallback`
- `Fast slash command`
- `Composer Intelligence Speed menu`
- `Disable automatic updates schema`
- `Disable automatic updates setting`

No Plugins target, `GPT-5.x model list`, or `GPT-5.6 model query selector`
target was applied by the version-filtered patcher.

Main-process source-signature validation changed the three settings-schema
copies plus the callback-aware Sparkle hook in
`.vite/build/window-all-closed-Bz3ZcBls.js`. All four patched main-process
sources parsed successfully. The injected updater source contained
`codexfastAutomaticUpdatesDisabled`, while `checkForUpdates` and
`installUpdatesIfAvailable` remained present so manual update actions are
preserved.

## Validation boundary

- Direct installed-bundle inspection, version-filtered extracted-bundle patching, JavaScript parse checks, main-process source-signature validation, and regression coverage were performed.
- The installed `ChatGPT.app` signature was valid before and after validation, and no installed application files were written.
- ChatGPT was running and hosted the validation session, so it was not forcibly quit and a fresh interactive runtime-launch smoke pass was not performed. Full launch and UI click-through remain separate manual checks from [`../real-app-validation.md`](../real-app-validation.md).
