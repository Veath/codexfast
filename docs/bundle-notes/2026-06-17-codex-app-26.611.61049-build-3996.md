# Codex.app 26.611.61049 Build 3996

## Summary

This note records the inspected bundle shape for the newly supported `Codex.app` build.

- Version: `26.611.61049`
- Build: `3996`
- Verification date: `2026-06-17`
- Result: `supported`

## Verified Feature Paths

- Composer `/fast` slash command
- Settings-side Fast control
- Composer `Intelligence` dropdown Speed submenu
- Plugins catalog and install paths for custom API users, with the old sidebar/page/detail API-key gates absent in the inspected bundle
- Curated OpenAI plugin catalog visibility, plugin install-button availability, plugin detail app-connect content, install-modal content, post-install app connect, and shared plugin marketplace prefetch gates
- Official GPT-5.5 model-list visibility is expected through the app path used by `26.422.30944` and later builds

## Bundle Notes

- Assets are still under `app/webview/assets` inside `app.asar`.
- The inspected target files are `general-settings-BrSCFSM1.js`, `composer-DWFAaSck.js`, `use-service-tier-settings-COAzInwV.js`, `check-plugin-availability-Dt8gcp_v.js`, `use-plugin-install-flow-CNEV_P5X.js`, `use-plugins-DCH1nRSg.js`, and `app-prefetch-impl-CEgE9TWt.js`.
- The Settings-side Fast gate still destructures `isServiceTierAllowed` from the availability hook and preserves `serviceTierSettings.availableOptions.length<=1`.
- The shared service-tier allowance hook in `use-service-tier-settings-COAzInwV.js` still treats `isServiceTierAllowed` as ChatGPT-only by default. `codexfast` must patch this source hook so custom API users can persist and send the selected Fast service tier, not only see the Settings and composer controls.
- The same service-tier hook gives conversation-level `serviceTier` state priority over Settings by default. `codexfast` must patch that fallback so reopened conversations use the configured Settings Fast tier instead of stale conversation-level Standard state.
- The `/fast` slash command is still generated from service-tier command entries in the composer bundle.
- The composer `Intelligence` Speed menu still uses a boolean `allowed && serviceTierSettings.availableOptions.length>1` gate. The existing patch removes only the Fast availability guard and preserves the option-count guard.
- The old Plugins sidebar disabled-nav target, `/skills` page-content auth block, and plugin detail redirect target were not present in the inspected bundle.
- Curated OpenAI plugin catalog visibility is still hidden for non-ChatGPT auth by the `openai-curated-marketplaces-hidden` helper in `use-plugins-DCH1nRSg.js`.
- The same `use-plugins-DCH1nRSg.js` path can still drop the curated catalog after `list-plugins` returns it when the `4218407052` vertical-catalog flag sets the marketplace exclusion list to `openai-curated`; `codexfast` patches that exclusion back to the empty list.
- Plugin install availability in `check-plugin-availability-Dt8gcp_v.js` now computes connector-level blocked reasons inside a loop, then derives aggregate `blockedReason` afterward. `codexfast` patches only the aggregate `connector-unavailable` branch to `null`, preserving connector-level reasons and the all-admin `disabled-by-admin` block.
- Plugin detail app rows in `check-plugin-availability-Dt8gcp_v.js` now enrich directory apps with plugin category metadata before filtering. `codexfast` patches the missing-directory `return null` branch to return plugin-declared fallback app rows.
- Plugin install modal content still gates basic modal details through a disclosure boolean.
- The post-install flow still closes immediately for `ON_USE` plugins before considering `appsNeedingAuth`, and the apps replacement continues to use the generic captured install-session setter name.
- Startup plugin prefetch still requests the remote `shared-with-me` marketplace in `app-prefetch-impl-CEgE9TWt.js`.

## Verification Notes

- Direct bundle inspection reported guarded matches for Settings Fast, service-tier allowance, service-tier conversation fallback, service-tier slash command, Intelligence Speed, both plugin catalog visibility gates, plugin install availability, plugin detail app connect, plugin install modal content, post-install app connect, and shared plugin marketplace prefetch targets.
- Direct bundle inspection did not find the old Plugins sidebar/page/detail API-key gates, so runtime launch does not require the `Plugins access` target for this exact build.
- Regression coverage includes the `26.611.61049` (`build 3996`) whitelist gate, the no-legacy-Plugins-required runtime launch rule, and the new plugin install availability and plugin detail app-connect signatures.
