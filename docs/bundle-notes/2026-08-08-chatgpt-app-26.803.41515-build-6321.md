# ChatGPT.app 26.803.41515 (build 6321)

- Strict compatibility key: `26.803.41515+6321`.
- Local bundle: `/Applications/ChatGPT.app`, bundle id `com.openai.codex`.
- Renderer Fast, service-tier, `/fast`, Intelligence Speed, and renderer settings-schema targets are in `webview/assets/app-initial-Biw83Aiz.js`; Settings Fast and the automatic-update row are in `webview/assets/general-settings-BseQIe_j.js`.
- Main-process settings schema copies are in `.vite/build/child-process-snapshot-worker.js`, `.vite/build/src-Cz_uUmVl.js`, and `.vite/build/worker.js`.
- The active Sparkle updater is `.vite/build/window-all-closed-9IR0zY5D.js`. Its background path uses `p=async(...)`, `setAutomaticBackgroundDownloadsEnabledForMac`, and `setInterval(()=>void p(),e)`, so the launcher uses the nested callback-aware source-signature hook. Manual update methods remain available.
- GPT-5.6 and Plugins use official application paths; runtime launch skips their compatibility target families.

Applying the version-filtered patcher to an extracted copy changed only the expected renderer and main-process modules. The installed app bundle was inspected read-only and was not modified or re-signed.
