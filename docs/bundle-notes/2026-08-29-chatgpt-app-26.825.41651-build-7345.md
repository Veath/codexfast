# ChatGPT.app 26.825.41651 (build 7345)

## Identification

- Bundle: `/Applications/ChatGPT.app`
- Bundle identifier: `com.openai.codex`
- `CFBundleShortVersionString`: `26.825.41651`
- `CFBundleVersion`: `7345`
- Inspected: `2026-08-29`

## Findings

- Renderer assets moved to `app-initial-Cw7fcqej.js` and `general-settings-C_w8yarq.js`. Existing Fast allowance, request-tier helper, configured-tier fallback, `/fast`, settings schema, and Settings Fast targets still match.
- The General Settings automatic-update row target matches the current `Ko` function shape and its replacement remains valid and idempotent.
- Main-process settings schema copies are in `.vite/build/src-4lLVrYxe.js` and `.vite/build/worker.js`. The active updater is `.vite/build/window-all-closed-D42BprKf.js`; source-signature discovery still matches its updater and forced-install paths.
- GPT-5.6 and Plugins remain official in this build. Runtime launch skips the GPT-5.6 injection/query-selector and Plugins target families for the exact `26.825.41651+7345` key.

## Validation

- Extracted `app.asar` read-only with `@electron/asar` into a temporary directory.
- Inspected 7,013 JavaScript files and applied the version-filtered runtime patcher to the complete extracted tree. The expected renderer and main-process targets matched while legacy Plugins and GPT-5.6 targets were skipped.
- Patched JavaScript parsed successfully, repeated patching remained idempotent, and the regression suite covers the current General Settings shape plus strict version and runtime-policy gates.
- The installed app bundle was not modified during extraction or patch validation.
- A fresh runtime launch and visible Settings UI smoke check were not run because ChatGPT was already running; the launcher still requires a fully quit app for that validation.
