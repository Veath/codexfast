# Codex.app 26.422.30944 Build 2080

## Summary

This note records the inspected bundle shape for the newly supported `Codex.app` build.

- Version: `26.422.30944`
- Build: `2080`
- Verification date: `2026-04-25`
- Result: `supported`

## Verified Feature Paths

- Settings-side Fast control
- Composer `/fast` slash command
- Composer `Intelligence` dropdown Speed submenu
- Plugins sidebar access for custom API users
- Official GPT-5.5 model-list visibility for custom API users in the validated environment

## Bundle Notes

- Assets are still under `app/webview/assets` inside `app.asar`.
- The Fast and Plugins target files and gate signatures matched the existing `26.422.21637` (`build 2056`) bundle shape during installed-app status detection.
- The Settings-side Fast target remains `general-settings-CnVD4YyB.js`.
- The slash command, composer `Intelligence` Speed menu, Plugins sidebar gate, and model-list bridge handler remain in `index-gATb9Tvd.js`.
- The visible model query selector remains in `font-settings-C9TXXljS.js`.
- User validation confirmed GPT-5.5 is visible before applying `codexfast` on this build.

## Gate Notes

- The Settings-side Fast control still uses the `P()` and `be()` guarded early-return shape.
- The composer `Intelligence` Speed menu is still gated by a local `_f()` assignment before rendering the Speed submenu.
- The Plugins sidebar disabled-nav state still derives from `$f("533078438")`, `authMethod==="apikey"`, and the adjacent `O&&k` local.
- The same sidebar block still derives the unified Skills/Plugins nav label state from `Ha({hostId:me})&&!k`.
- The model-list bridge still delegates to `model/list`.
- The model query selector still filters raw models into `modelsByType.models`, but this build and later builds are expected not to require the `codexfast` GPT-5.5 injection because GPT-5.5 is visible through the official app path in the validated environment.

## Verification Notes

- Real installed-app status detection found the Fast, Plugins, and GPT-5.5 compatibility signatures in the local bundle.
- Shell regression coverage was expanded in `test/re-sign-flow.sh` for the `26.422.30944` (`build 2080`) whitelist gate using the matching 26.422 bundle shape.
- Shell regression coverage now verifies that apply skips the GPT-5.5 model-list and query-selector targets on this build.
- Shell regression coverage now verifies that later versions also omit unpatched GPT-5.5 compatibility targets.
- Shell regression coverage now verifies that restore still removes earlier `0.5.2` GPT-5.5 patch markers on this build when archive backup restore is unavailable.
- The strict compatibility whitelist now includes `26.422.30944+2080`.
- Real installed-app apply/restore was not run during this adaptation; use the manual checklist in `docs/real-app-validation.md` before making broader real-app behavior claims.
