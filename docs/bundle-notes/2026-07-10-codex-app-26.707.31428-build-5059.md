# Codex.app 26.707.31428 Build 5059

## Summary

This note records the inspected bundle shape for the local `com.openai.codex` app installed as `ChatGPT.app`.

- Version: `26.707.31428`
- Build: `5059`
- Verification date: `2026-07-10`
- Local app path: `/Applications/ChatGPT.app`
- Result: `supported`

## Verified Feature Paths

- Composer `/fast` slash command
- Settings-side Fast control
- Composer `Intelligence` dropdown Speed submenu
- Settings > General `Disable automatic updates` row, plus dynamic background Sparkle check and forced automatic install suppression through the launcher main-process hook
- Official Plugins support through the app path, with codexfast Plugins runtime targets skipped for this build
- GPT-5.5 Fast metadata augmentation
- GPT-5.6 Sol, Terra, and Luna model-list entry/metadata injection with Max reasoning on all three and Ultra on Sol/Terra only

## Bundle Notes

- Assets are still under `app/webview/assets` inside `app.asar`.
- The installed app uses `CFBundleName` `ChatGPT`, `CFBundleIdentifier` `com.openai.codex`, and executable `Contents/MacOS/ChatGPT`. Runtime launch must support both `Codex` and `ChatGPT` process/executable names.
- Settings-side Fast moved to `general-settings-J6e_rcRy.js` and still uses the destructured `isServiceTierAllowed` gate with `serviceTierSettings.availableOptions.length<=1`.
- The shared service-tier allowance target moved to `app-initial~app-main~onboarding-page~hotkey-window-thread-page~quick-chat-window-page~chatg~k0ede4gb-C17KDkOa.js`.
- The service-tier request helper target moved to `app-initial~app-main~pull-request-code-review~onboarding-page~hotkey-window-thread-page~cha~b76hmflu-y0KJWbm3.js`. The helper now uses `if(authMethod !== "chatgpt") return false`, so codexfast patches that non-ChatGPT branch to allow custom providers while preserving ChatGPT `fast_mode` requirements.
- The conversation fallback target moved to `app-initial~app-main~onboarding-page~hotkey-window-thread-page~quick-chat-window-page~chatg~k0ede4gb-C17KDkOa.js`. The hook now includes new-thread draft settings before the conversation branch, so codexfast preserves the draft/default path while removing stale conversation/latest-turn Standard overrides.
- The `/fast` slash command and composer `Intelligence` Speed menu target moved to `app-initial~app-main~new-thread-panel-page~appgen-library-page~hotkey-window-thread-page~ho~iufn7mg3-Cdmi2Vi6.js`.
- The model-list handler moved to `app-initial~app-main~page-CtX5-cLy.js` and now exposes a priority/source/timeout wrapper: `"list-models-for-host":Q7((manager,{priority,source,timeoutMs,...params})=>manager.sendRequest("model/list",params,{priority,source,timeoutMs}))`. codexfast wraps that shape to preserve request options while adding or normalizing GPT-5.6 Sol, Terra, and Luna plus GPT-5.5 Fast metadata. The wrapper uses codexfast-prefixed locals because a fixed short result variable can collide with minified request-option parameters.
- The inspected bundle contains static GPT-5.6 Sol/Terra power-setting data and localized labels for Max and Ultra. The model-list bridge supplies the complete custom-provider catalog matrix: Sol/Terra low/medium/high/xhigh/max/ultra, and Luna low/medium/high/xhigh/max without ultra.
- Live renderer inspection showed that the raw TanStack Query cache already contained all three injected GPT-5.6 entries, but the selected composer model list omitted them. Dynamic config `107580212` had `use_hidden_models: true` with an `available_models` allowlist limited to GPT-5.5/5.4/5.4-mini/5.3-codex/5.2, and the selector separately filtered enabled efforts. The `gpt56-model-query-selector` target augments that allowlist with Sol/Terra/Luna, adds max/ultra to the effort set, and forces Ultra through to per-model metadata filtering.
- The automatic-update Settings row matches `disable-automatic-updates-setting-26707-app-name` because the Prevent Sleep description changed to `Keep your computer awake while {appName} is running a task`.
- The automatic-update settings schema target still matches `.vite/build/child-process-snapshot-worker.js`.
- Several older Plugins catalog/install/app-connect signatures are still detectable by the generic target inspector, but this build supports Plugins through the official app path. Runtime launch filters out all Plugins target ids for `26.707.31428+5059`.

## Verification Notes

- `Info.plist` reported `CFBundleShortVersionString` `26.707.31428` and `CFBundleVersion` `5059` from `/Applications/ChatGPT.app/Contents/Info.plist`.
- Direct bundle inspection reported guarded matches for Settings Fast, service-tier allowance, the new service-tier request allowance helper, the new conversation fallback, service-tier slash command, composer `Intelligence` Speed through the code-level target, the automatic-update schema target, the app-name-aware automatic-update General settings row target, and the GPT-5.x model-list handler.
- Regression coverage includes the `26.707.31428` (`build 5059`) whitelist gate, ChatGPT executable fallback path reporting, the no-legacy-Plugins-required runtime launch rule, valid generated JavaScript for the model-list wrapper, the GPT-5.6 Sol/Terra/Luna effort matrix with Luna excluding Ultra, the downstream model/effort selector and its idempotency, and the runtime patcher wrapper that skips Plugins target ids while preserving non-Plugins targets for this build.
