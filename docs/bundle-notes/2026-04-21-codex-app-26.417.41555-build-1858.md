# Codex.app 26.417.41555 Build 1858

## Summary

This note records the inspected bundle shape for the adapted `Codex.app` build.

- Version: `26.417.41555`
- Build: `1858`
- Verification date: `2026-04-21`
- Result: `supported`

## Verified Feature Paths

- Settings-side Fast control
- Composer `/fast` slash command
- `Add files and more / +` Speed submenu
- Plugins sidebar access for custom API users

## Bundle Notes

- Assets are still under `app/webview/assets` inside `app.asar`.
- The Settings-side Fast target moved to `general-settings-D2eks1ok.js`.
- The slash command target remained in `index-CxBol07n.js`.
- The add-context Speed menu target moved to `use-model-settings-ldiRRtPt.js`.
- The Plugins sidebar gate was still in the sidebar/index bundle area, with the inspected gated shape isolated in a sidebar-local asset during adaptation.

## Gate Notes

- The Settings-side Fast control now uses `ae()` and `se()` in the guarded early-return shape.
- The add-context Speed menu now uses `cr()` and `jr(...)` in the gate plus service-tier binding sequence.
- The Plugins sidebar gate derives the disabled-nav state from `O=hf("533078438")`, `A=authMethod==="apikey"`, and `j=O&&A`.
- The same sidebar block also derives the unified Skills/Plugins nav label state from `D=Fs()` and `M=D&&!A`.

## Verification Notes

- Shell regression coverage was expanded in `test/re-sign-flow.sh` for the 26.417 bundle shapes.
- The strict compatibility whitelist now includes `26.417.41555+1858`.
- Real-app validation was rerun on `2026-04-21`, including apply, restart, restore, and a follow-up re-apply that normalized the legacy partial Plugins patch state.
- The post-fix real-app sidebar shows a visible `Plugins` entry after restart.
