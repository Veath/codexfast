# Codex.app 26.415.40636 Build 1799

## Summary

This note records the inspected bundle shape for the currently supported `Codex.app` build.

- Version: `26.415.40636`
- Build: `1799`
- Verification date: `2026-04-20`
- Result: `supported`

## Verified Feature Paths

- Settings-side Fast control
- Composer `/fast` slash command
- `Add files and more / +` Speed submenu
- Plugins sidebar access for custom API users

## Bundle Notes

- Assets are still under `app/webview/assets` inside `app.asar`.
- The Settings-side Fast target was matched in `general-settings-BZQqrI-r.js`.
- The slash command and Plugins sidebar gate were matched in `index-n7COQvZQ.js`.
- The add-context Speed menu target was matched in `use-model-settings-DEaRTAXy.js`.

## Gate Notes

- Plugins sidebar access was gated by a local `authMethod === "apikey"` branch in the sidebar flow.
- The current narrow patch removes that gate in the matched local assignment instead of rewriting the broader sidebar logic.

## Verification Notes

- Shell regression coverage exists in `test/re-sign-flow.sh`.
- The current strict compatibility whitelist includes `26.415.40636+1799`.
- Real-app behavior was validated alongside restore and re-sign behavior during this adaptation cycle.
