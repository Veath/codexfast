# Codex.app 26.422.21637 Build 2056

## Summary

This note records the inspected bundle shape for the newly supported `Codex.app` build.

- Version: `26.422.21637`
- Build: `2056`
- Verification date: `2026-04-24`
- Result: `supported`

## Verified Feature Paths

- Settings-side Fast control
- Composer `/fast` slash command
- Composer `Intelligence` dropdown Speed submenu
- Plugins sidebar access for custom API users
- GPT-5.5 model-list entry for custom API users

## Bundle Notes

- Assets are still under `app/webview/assets` inside `app.asar`.
- The Settings-side Fast target moved to `general-settings-CnVD4YyB.js`.
- The slash command target moved to `index-gATb9Tvd.js`.
- The previous add-context Speed target is no longer the active composer-side Speed path for this build.
- The composer-side Speed target is now in `index-gATb9Tvd.js` under the `composer.intelligenceDropdown.speed.title` message block.
- The Plugins sidebar gate is also in `index-gATb9Tvd.js`.
- The model-list bridge handler is in `index-gATb9Tvd.js` under the `"list-models-for-host"` handler.
- The model query selector used by the visible menu is in `font-settings-C9TXXljS.js`; it transforms the raw list into `modelsByType.models`.

## Gate Notes

- The Settings-side Fast control now uses `P()` and `be()` in the guarded early-return shape.
- The composer `Intelligence` Speed menu is gated by a local `_f()` assignment before rendering the Speed submenu.
- The Plugins sidebar disabled-nav state derives from `O=$f("533078438")`, `k=authMethod==="apikey"`, and `A=O&&k`.
- The same sidebar block derives the unified Skills/Plugins nav label state from `ee=Ha({hostId:me})&&!k`.
- The model-list bridge delegates to `model/list`; the patch wraps that handler and appends `gpt-5.5` only when the returned data array does not already include it.
- The model query selector can still filter the injected raw entry out through `useHiddenModels` and `availableModels`; the selector patch appends `gpt-5.5` after that filter so the visible model picker receives it.

## Verification Notes

- Shell regression coverage was expanded in `test/re-sign-flow.sh` for the 26.422 bundle shapes.
- Shell regression coverage now checks apply and restore behavior for both the `GPT-5.5 model list` and `GPT-5.5 model query selector` targets.
- The strict compatibility whitelist now includes `26.422.21637+2056`.
- Real installed-app status detection was run on `2026-04-24` and found the supported targets in the local bundle.
- Real installed-app apply/restore was not run during this adaptation; use the manual checklist in `docs/real-app-validation.md` before making broader real-app behavior claims.
