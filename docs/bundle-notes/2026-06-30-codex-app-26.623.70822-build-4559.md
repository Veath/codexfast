# Codex.app 26.623.70822 Build 4559

## Summary

This note records the inspected bundle shape for the newly supported `Codex.app` build.

- Version: `26.623.70822`
- Build: `4559`
- Verification date: `2026-06-30`
- Result: `supported`

## Verified Feature Paths

- Composer `/fast` slash command
- Settings-side Fast control
- Composer `Intelligence` dropdown Speed submenu
- Settings > General `Disable automatic updates` row, plus dynamic background Sparkle check suppression through the launcher main-process hook
- Official Plugins support through the app path, with codexfast Plugins runtime targets skipped for this build
- Official GPT-5.5 model-list visibility is expected through the app path used by `26.422.30944` and later builds

## Bundle Notes

- Assets are still under `app/webview/assets` inside `app.asar`.
- Settings-side Fast moved to `general-settings-CyYw_gZ3.js` and still uses the destructured `isServiceTierAllowed` gate with `serviceTierSettings.availableOptions.length<=1`.
- The shared service-tier allowance and conversation fallback targets moved to `app-initial~app-main~onboarding-page-CgNc-Bk2.js`.
- The request service-tier allowance target moved to `app-initial~app-main~worktree-init-v2-page~remote-conversation-page~new-thread-panel-page~o~kg2pu5rs-N3llppXI.js`.
- The `/fast` slash command and composer `Intelligence` Speed menu target moved to `app-initial~app-main~remote-conversation-page~new-thread-panel-page~appgen-library-page~hot~djo67r4n-CrVrqCBe.js`.
- The automatic-update Settings row still uses the `disable-automatic-updates-setting-26623-renamed-locals` target in `general-settings-CyYw_gZ3.js`.
- The automatic-update Settings row can otherwise crash because the bundle reads the outer settings atom as `t=c(o)` before a copied row-local declaration `let o,s`. The replacement must use codexfast-prefixed local names for the cache, settings state, labels, descriptions, checked values, and callbacks instead of reusing any minified row locals.
- The automatic-update settings schema target matches `.vite/build/child-process-snapshot-worker.js`.
- The launcher-side Sparkle hook target still matches `.vite/build/workspace-root-drop-handler-DeLi4ACJ.js`.
- The old Plugins sidebar disabled-nav target, `/skills` page-content auth block, and plugin detail redirect target were not present in the inspected bundle.
- Several older Plugins catalog/install/app-connect signatures are still detectable by the generic target inspector, but this build supports Plugins through the official app path. Runtime launch filters out all Plugins target ids for `26.623.70822+4559`.

## Verification Notes

- `Info.plist` reported `CFBundleShortVersionString` `26.623.70822` and `CFBundleVersion` `4559` from `/Applications/Codex.app/Contents/Info.plist`.
- Before the compatibility update, `node bin/codexfast launch` reported `Compatibility: unsupported` for `26.623.70822+4559`.
- Direct bundle inspection reported guarded matches for Settings Fast, service-tier allowance, service-tier request allowance, service-tier conversation fallback, service-tier slash command, composer `Intelligence` Speed through the code-level target, the automatic-update schema target, and the `disable-automatic-updates-setting-26623-renamed-locals` General settings target.
- Direct bundle inspection did not find the old Plugins sidebar/page/detail API-key gates.
- Regression coverage includes the `26.623.70822` (`build 4559`) whitelist gate, the no-legacy-Plugins-required runtime launch rule, the runtime patcher wrapper that skips Plugins target ids while preserving non-Plugins targets for this build, and the Settings atom argument collision shape that previously produced `t=c(o)` plus `let o,s`.
