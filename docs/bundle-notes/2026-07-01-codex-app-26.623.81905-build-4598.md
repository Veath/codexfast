# Codex.app 26.623.81905 Build 4598

## Summary

This note records the inspected bundle shape for the newly supported `Codex.app` build.

- Version: `26.623.81905`
- Build: `4598`
- Verification date: `2026-07-01`
- Result: `supported`

## Verified Feature Paths

- Composer `/fast` slash command
- Settings-side Fast control
- Composer `Intelligence` dropdown Speed submenu
- Settings > General `Disable automatic updates` row, plus dynamic background Sparkle check and forced automatic install suppression through the launcher main-process hook
- Official Plugins support through the app path, with codexfast Plugins runtime targets skipped for this build
- GPT-5.5 model-list Fast metadata augmentation

## Bundle Notes

- Assets are still under `app/webview/assets` inside `app.asar`.
- Settings-side Fast moved to `general-settings-BMV5mko2.js` and still uses the destructured `isServiceTierAllowed` gate with `serviceTierSettings.availableOptions.length<=1`.
- The shared service-tier allowance, request allowance, and conversation fallback targets moved to `app-initial~app-main~onboarding-page-DewXrzLR.js`.
- The `/fast` slash command and composer `Intelligence` Speed menu target moved to `app-initial~app-main~remote-conversation-page~new-thread-panel-page~appgen-library-page~hot~djo67r4n-BuWR8hI_.js`.
- The model-list handler moved to `app-initial~app-main~automations-page-Da2pvR4p.js` and now exposes the simple `"list-models-for-host":$7((e,t)=>e.sendRequest(`model/list`,t))` shape that `codexfast` wraps to augment GPT-5.5 Fast metadata.
- The automatic-update Settings row moved to a hook-based `La()` shape in `general-settings-BMV5mko2.js`. It reads settings through `B(j)`, reads `preventSleepWhileRunning` through `r(p.preventSleepWhileRunning)`, and writes through `V(t,p.preventSleepWhileRunning,e)`, so it uses the `disable-automatic-updates-setting-26623-hooks` target.
- The automatic-update settings schema target matches `.vite/build/child-process-snapshot-worker.js`.
- The launcher-side Sparkle hook target moved to `.vite/build/workspace-root-drop-handler-4fzIumU3.js`.
- The old Plugins sidebar disabled-nav target, `/skills` page-content auth block, and plugin detail redirect target were not present in the inspected bundle.
- Several older Plugins catalog/install/app-connect signatures are still detectable by the generic target inspector, but this build supports Plugins through the official app path. Runtime launch filters out all Plugins target ids for `26.623.81905+4598`.

## Verification Notes

- `Info.plist` reported `CFBundleShortVersionString` `26.623.81905` and `CFBundleVersion` `4598` from `/Applications/Codex.app/Contents/Info.plist`.
- Direct bundle inspection reported guarded matches for Settings Fast, service-tier allowance, service-tier request allowance, service-tier conversation fallback, service-tier slash command, composer `Intelligence` Speed through the code-level target, the automatic-update schema target, the hook-based automatic-update General settings target, and the simple GPT-5.5 model-list handler.
- Direct bundle inspection initially reported the automatic-update General settings target and GPT-5.5 model-list target as needle-only, which showed that `26.623.70822`'s row matcher did not match the new hook-based `La()` row shape and the model-list matcher did not cover the new `$7` wrapper local.
- Regression coverage includes the `26.623.81905` (`build 4598`) whitelist gate, the no-legacy-Plugins-required runtime launch rule, the runtime patcher wrapper that skips Plugins target ids while preserving non-Plugins targets for this build, and the hook-based Settings row shape for the automatic-update row.
