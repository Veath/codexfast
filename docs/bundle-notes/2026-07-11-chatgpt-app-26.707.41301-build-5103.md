# ChatGPT.app 26.707.41301 build 5103

## Bundle identity

- Installed app: `/Applications/ChatGPT.app`
- Bundle id: `com.openai.codex`
- `CFBundleShortVersionString`: `26.707.41301`
- `CFBundleVersion`: `5103`
- Strict compatibility key: `26.707.41301+5103`

## Compatibility conclusions

- Plugins remains supported by the official app path, so runtime launch skips all Plugins targets for this build.
- GPT-5.6 is supported by the official app path. Starting at this version/build threshold, separately whitelisted builds skip `gpt5x-model-list-options` and `gpt56-model-query-selector` instead of injecting model-list entries or widening the downstream query selector.
- Earlier supported builds, including `26.707.31428+5059`, retain the GPT-5.6 compatibility patches.
- The strict exact-version/build whitelist is unchanged. A later build is not accepted merely because it is numerically newer; it must still be inspected and added to `src/supported-app-versions.mts`.

## Automatic-update findings

- The Settings asset moved to `webview/assets/general-settings-BBi3jMJr.js` and uses a new platform-local layout around the existing Prevent Sleep row.
- The Sparkle manager moved to `.vite/build/sqlite-BqLffnB9.js`.
- The previous main-process hook selected historical `workspace-root-drop-handler-*.js` and `src-*.js` basenames, so it never inspected the moved Sparkle module. The visible setting could therefore be enabled while background update checks and forced automatic install scheduling still ran.
- Main-process discovery now inspects JavaScript modules under `.vite/build` and patches only modules whose source contains the updater or desktop-settings schema signatures.
- Manual `Check for Updates` and `installUpdatesIfAvailable` paths remain available because the updater is still initialized and only automatic triggers are gated.

## Validation boundary

- Direct installed-bundle inspection and extracted-bundle target inspection were performed against the local build.
- Regression coverage includes the strict whitelist, official-GPT-5.6 threshold behavior for older/current/later version keys, build-5103 Settings syntax and idempotency, content-driven main-process discovery, dynamic automatic-update gates, and preservation of manual update actions.
- A fresh real-app runtime launch was not performed while an active ChatGPT/Codex session could be interrupted. The installed app bundle was kept read-only and must retain its original signature and version metadata.
