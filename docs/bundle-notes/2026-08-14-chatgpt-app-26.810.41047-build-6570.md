# ChatGPT.app 26.810.41047 (build 6570)

- Strict compatibility key: `26.810.41047+6570`.
- Local bundle: `/Applications/ChatGPT.app`, bundle id `com.openai.codex`, executable `ChatGPT`.
- Renderer Fast, service-tier allowance, request-tier allowance, configured-tier fallback, `/fast`, and renderer settings-schema targets match `webview/assets/app-initial-iMhn6nFd.js`.
- Settings Fast and the automatic-update row match `webview/assets/general-settings-mYHA2JS4.js`.
- Main-process settings schema copies match `.vite/build/child-process-snapshot-worker.js`, `.vite/build/src-BlUt09P1.js`, and `.vite/build/worker.js`.
- The active Sparkle updater matches `.vite/build/window-all-closed-B4DXN-uV.js`; the existing source-signature hook remains applicable and preserves manual update actions.
- The Intelligence Speed component renders from available service-tier options and no longer contains the historical availability gate, so no new Intelligence Speed target is required.
- GPT-5.6 and Plugins use official application paths. Runtime launch skips their compatibility target families for this exact version/build.
- Validation is read-only against a temporary extraction of `app.asar`; public `launch` does not modify the installed app bundle, `Info.plist`, or its signature.
- The currently running ChatGPT/Codex process was not closed, terminated, restarted, or relaunched during this adaptation.
