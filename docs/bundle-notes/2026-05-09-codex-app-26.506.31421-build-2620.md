# Codex.app 26.506.31421 Build 2620

## Summary

This note records the inspected bundle shape for the newly supported `Codex.app` build.

- Version: `26.506.31421`
- Build: `2620`
- Verification date: `2026-05-09`
- Result: `supported`

## Verified Feature Paths

- Composer `/fast` slash command
- Settings-side Fast control
- Composer `Intelligence` dropdown Speed submenu
- Plugins sidebar access for custom API users
- Plugins page content, plugin detail deep links, install-button availability, and install-modal content gates
- Official GPT-5.5 model-list visibility is expected through the app path used by `26.422.30944` and later builds

## Bundle Notes

- Assets are still under `app/webview/assets` inside `app.asar`.
- The inspected target files are `general-settings-S-ejdEdp.js`, `composer-DawxvKsB.js`, `app-main-Bucm979x.js`, `skills-page-CrAHPBnF.js`, `plugin-detail-page-8pMyEW6A.js`, and `plugins-availability-DW4iBzVW.js`.
- The Settings-side Fast gate still uses `ie()` for availability and `de()` for the service-tier hook.
- The Fast slash command remains in a `composer-*.js` asset and still uses the existing `enabled:<local>` target shape.
- The composer `Intelligence` Speed submenu still gates rendering through `va(...)` and sits next to `fi(_T,n)`.
- The Plugins sidebar disabled-nav state now uses `Qo("533078438")` with `Xc(authMethod)`.
- The unified Skills/Plugins nav label state now uses `Nc({hostId:Ya})` and an API-key exclusion.
- The `/skills` route page-content gate still matches the existing API-key-derived local assignment shape.
- The plugin detail redirect still uses an auth-state hook followed by an API-key redirect guard.
- The plugin install availability and install modal content gates remain in `plugins-availability-*.js`.
- The model-list bridge still delegates to `model/list`, but `codexfast` skips GPT-5.5 apply targets on this build because GPT-5.5 is expected to be visible through the official app path from `26.422.30944` onward.

## Gate Notes

- The Settings-side Fast control still uses the guarded `if(!n)return null;` shape.
- The Fast slash command still uses `enabled:a`.
- The composer `Intelligence` Speed menu renders only when the local `va(...)` result is truthy.
- The Plugins sidebar disabled-nav state uses `d=e&&l&&u`.
- The unified Skills/Plugins nav label state uses `p=e&&f&&!u`.
- The Plugins page content state uses `let p=d` after the API-key auth helper result.
- The plugin detail page redirect uses `if(ge(i))` after `authMethod` is read.
- The plugin install flow derives the top-level blocked reason from ``ee=h.length>0&&w===h.length?C?`disabled-by-admin`:`connector-unavailable`:null``; the patch keeps only the `disabled-by-admin` aggregate block.
- The plugin install modal content gate derives from the plugin detail apps and `ON_INSTALL` auth policy; the patch forces that local gate false so About, Includes, and Capabilities can render normally.

## Verification Notes

- Real installed-app status detection initially reported the build as unsupported before the whitelist update.
- Direct bundle inspection confirmed the supported feature paths are still present with renamed asset files and an updated Plugins sidebar gate signature.
- Real installed-app apply patched eight targets, created `app.asar1`, updated the Electron ASAR integrity hash, re-signed successfully, and reset the Codex screen-recording permission record.
- Follow-up real installed-app status reported the Settings Fast, `/fast`, composer `Intelligence` Speed, Plugins sidebar, Plugins page content, plugin detail, plugin install availability, and plugin install modal content targets as enabled.
- `codesign --verify --deep --strict --verbose=2 /Applications/Codex.app` passed after patching, and the app remained in packed `app.asar` layout without a persistent `Contents/Resources/app` directory.
- Regression coverage includes the `26.506.31421` (`build 2620`) whitelist gate and apply/restore behavior for the Settings, Intelligence Speed, sidebar Plugins, Plugins page content, plugin detail, plugin install availability, and plugin install modal content target shapes.
