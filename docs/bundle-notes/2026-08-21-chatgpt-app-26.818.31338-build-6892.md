# ChatGPT.app 26.818.31338 (build 6892)

- Strict compatibility key: `26.818.31338+6892`.
- The installed app bundle id remains `com.openai.codex`; inspection used `/Applications/ChatGPT.app` read-only while the current app process remained running.
- Renderer Fast targets consolidated in `webview/assets/app-initial-B2RlNf_b.js`: the shared service-tier allowance, three-argument request allowance helper, configured-Settings-tier fallback, `/fast`, and renderer settings schema all match existing signatures.
- Settings Fast moved to `webview/assets/general-settings-DOzsH83_.js` and matches the existing destructured option-count signature.
- The composer Intelligence Speed menu now consumes `isServiceTierAllowed` and `availableOptions.length > 1` directly, so enabling the shared allowance is sufficient and no additional menu regex is needed.
- GPT-5.6 and Plugins continue to use official application paths; the version-filtered runtime patcher skips both compatibility target families.
- The renderer still has no `disableAutomaticUpdates` row. Main-process schema signatures match `.vite/build/src-Bqg9CB1K.js` and `.vite/build/worker.js`, and the active updater signature matches `.vite/build/window-all-closed-BazhJdtt.js`.
- Version-filtered patches were applied to the extracted sources twice to confirm idempotency, and the patched renderer and updater modules passed JavaScript syntax checks.
- The installed `app.asar`, `Info.plist`, and Developer ID signature were not modified. A real runtime launch and visible Settings/Plugins UI walkthrough were not performed because ChatGPT was already running.
