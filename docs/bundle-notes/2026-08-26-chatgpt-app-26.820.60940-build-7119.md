# ChatGPT.app 26.820.60940 (build 7119)

## Identification

- Bundle: `/Applications/ChatGPT.app`
- Bundle identifier: `com.openai.codex`
- `CFBundleShortVersionString`: `26.820.60940`
- `CFBundleVersion`: `7119`
- Inspected: `2026-08-26`

## Findings

- Renderer assets moved to `app-initial-CmWKLN1D.js` and `general-settings-C6MQqx12.js`. Existing Fast service-tier allowance, request helper, configured-tier fallback, `/fast`, settings schema, and Settings Fast targets still match.
- The dedicated General Settings automatic-update target still matches, but its replacement failed because the prior capture expected the row result local to be named `p`; build 7119 names it `f`. The replacement now captures any valid minified row local while retaining the surrounding JSX row signature.
- Main-process settings schema copies are in `.vite/build/src-DpTP2OT9.js` and `.vite/build/worker.js`. The active updater is `.vite/build/window-all-closed-CZBXnSMj.js`; the existing source-signature discovery hook matches its nested callback/interval shape.
- GPT-5.6 and Plugins remain official in this build. Runtime launch skips the GPT-5.6 injection/query-selector and Plugins target families for the exact `26.820.60940+7119` key.

## Validation

- Extracted `app.asar` read-only with `@electron/asar` into a temporary directory.
- Inspected 6,887 JavaScript files and applied the version-filtered runtime patcher to the complete extracted tree. The expected renderer and main-process targets matched while legacy Plugins and GPT-5.6 targets were skipped.
- Patched JavaScript parsed successfully, repeated patching remained idempotent, and the regression suite covers the renamed General Settings row local plus the strict version and runtime-policy gates.
- The installed app bundle was not modified during extraction or patch validation.
- A fresh runtime launch and visible Settings UI smoke check were not run because ChatGPT was already running; the launcher correctly requires a fully quit app for that validation.
