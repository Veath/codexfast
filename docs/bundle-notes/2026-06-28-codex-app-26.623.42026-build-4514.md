# Codex.app 26.623.42026 Build 4514

## Summary

This note records the inspected bundle shape for the newly supported `Codex.app` build.

- Version: `26.623.42026`
- Build: `4514`
- Verification date: `2026-06-28`
- Result: `supported`

## Verified Feature Paths

- Composer `/fast` slash command
- Settings-side Fast control
- Composer `Intelligence` dropdown Speed submenu
- Settings > General `Disable automatic updates` row, plus dynamic background Sparkle check suppression through the launcher main-process hook
- Official Plugins support through the app path, with codexfast Plugins runtime targets skipped for this build
- Official GPT-5.5 model-list visibility is expected through the app path used by `26.422.30944` and later builds

## Bundle Notes

- Assets are still under `app/webview/assets` inside `app.asar`.
- Settings-side Fast moved to `general-settings-DeOIy93M.js` and still uses the destructured `isServiceTierAllowed` gate with `serviceTierSettings.availableOptions.length<=1`.
- The shared service-tier allowance, request allowance, and conversation fallback targets moved to `app-initial~app-main~onboarding-page-BUwCKIcU.js`.
- The `/fast` slash command and composer `Intelligence` Speed menu target moved to `app-initial~app-main~remote-conversation-page~new-thread-panel-page~appgen-library-page~hot~djo67r4n-CFm41aDU.js`.
- The automatic-update Settings row still uses the `disable-automatic-updates-setting-26623` family in `general-settings-DeOIy93M.js`, but the minified local names collide with the older matcher assumptions, so `disable-automatic-updates-setting-26623-local-collision` handles this build.
- The automatic-update settings schema target matches `.vite/build/child-process-snapshot-worker.js`.
- The launcher-side Sparkle hook target moved to `.vite/build/workspace-root-drop-handler-DbDnwAf_.js`; the inspected background check shape is `let f=XB();f>0&&setInterval(d,f).unref(),d()`. The same raw background check function is also reachable through `setAutomaticBackgroundDownloadsEnabledForMac=e=>{c.setAutomaticBackgroundDownloadsEnabled(e),e&&d()}`, so the launcher hook must route that automatic background download gate through the same `disableAutomaticUpdates` config check.
- The old Plugins sidebar disabled-nav target, `/skills` page-content auth block, and plugin detail redirect target were not present in the inspected bundle.
- Several older Plugins catalog/install/app-connect signatures are still detectable by the generic target inspector, but this build supports Plugins through the official app path. Runtime launch filters out all Plugins target ids for `26.623.42026+4514`.

## Verification Notes

- `Info.plist` reported `CFBundleShortVersionString` `26.623.42026` and `CFBundleVersion` `4514` from `/Applications/Codex.app/Contents/Info.plist`.
- Before the compatibility update, `bash test/re-sign-flow.sh` reported `Compatibility: unsupported` for `26.623.42026+4514`.
- Direct bundle inspection reported guarded matches for Settings Fast, service-tier allowance, service-tier request allowance, service-tier conversation fallback, service-tier slash command, composer `Intelligence` Speed through the code-level target, the automatic-update schema target, and the `disable-automatic-updates-setting-26623-local-collision` General settings target.
- Direct bundle inspection did not find the old Plugins sidebar/page/detail API-key gates.
- Regression coverage includes the `26.623.42026` (`build 4514`) whitelist gate, the no-legacy-Plugins-required runtime launch rule, the runtime patcher wrapper that skips Plugins target ids while preserving non-Plugins targets for this build, the local-name-collision shape for the automatic-update row, and the Sparkle automatic background download gate path that can otherwise bypass `Disable automatic updates`.
