# ChatGPT.app 26.715.72359 build 5718

## Bundle metadata

- Installed app: `/Applications/ChatGPT.app`
- Bundle identifier: `com.openai.codex`
- `CFBundleShortVersionString`: `26.715.72359`
- `CFBundleVersion`: `5718`
- Strict compatibility key: `26.715.72359+5718`
- `app.asar` SHA-256 before validation: `6c6528eb1e8450cdc506a59586f8caffe87576e200977e2a11bdea0cecf1c718`
- `Info.plist` SHA-256 before validation: `adb60978602589adab1397158c1986b35a3c7ecd82201220690c1338d53a2b3f`
- Both hashes were unchanged after validation, and `codesign --verify --deep --strict` passed.

## Renderer findings

- Settings Fast and the automatic-update row: `webview/assets/general-settings-CM4Mcgcy.js`
- Shared Fast allowance and configured-tier fallback: the `l46phxln-D0OL-Wu_.js` shared chunk
- Request-tier allowance: the `b0wqjrxp-BxI_oO_W.js` shared chunk
- `/fast` and Intelligence Speed: the `unq8yzli-B-Ca7uGT.js` shared chunk
- Automatic-update Settings target: existing `disable-automatic-updates-setting-26715-row-local-h`
- GPT-5.6: official application path; compatibility injection and selector widening are skipped
- Plugins: official application path; Plugins runtime targets and the legacy required initial label are skipped

Applying the version-filtered build-5718 patcher in memory across the extracted
`webview/assets` and `.vite/build` JavaScript changed eight modules. The five
renderer-side changes were the shared Fast allowance/fallback chunk, the
Intelligence Speed and slash-command chunk, the request-tier chunk, the
Settings chunk, and one desktop-settings schema copy. Three additional
desktop-settings schema copies changed under `.vite/build`. The patched ESM
and script bodies all parsed successfully with the TypeScript JavaScript
parser. The exact patched-label set was:

- `Speed setting`
- `Speed service tier allowance`
- `Speed service tier request allowance`
- `Speed service tier conversation fallback`
- `Fast slash command`
- `Composer Intelligence Speed menu`
- `Disable automatic updates schema`
- `Disable automatic updates setting`

No Plugins target, `GPT-5.x model list`, or `GPT-5.6 model query selector`
target was applied by the version-filtered patcher.

Main-process source-signature validation changed the three settings-schema
copies in `.vite/build/child-process-snapshot-worker.js`,
`.vite/build/src-DVXSULz2.js`, and `.vite/build/worker.js`, plus the callback-
aware Sparkle hook in `.vite/build/window-all-closed-DoNbesKf.js`. The
`checkForUpdates` and `installUpdatesIfAvailable` manual update methods
remained present after in-memory patching.

## Validation boundary

- Direct installed-bundle inspection, version-filtered extracted-bundle patching, JavaScript/ESM parse checks, main-process source-signature validation, and regression coverage were performed.
- The installed `ChatGPT.app` signature was valid before and after validation, and no installed app files were written.
- ChatGPT was running and hosted the validation session, so it was not forcibly quit and a fresh interactive runtime-launch smoke pass was not performed. Full launch and UI click-through remain separate manual checks from [`../real-app-validation.md`](../real-app-validation.md).
