# ChatGPT.app 26.715.70719 build 5650

## Bundle metadata

- Installed app: `/Applications/ChatGPT.app`
- Bundle identifier: `com.openai.codex`
- `CFBundleShortVersionString`: `26.715.70719`
- `CFBundleVersion`: `5650`
- Strict compatibility key: `26.715.70719+5650`
- `app.asar` SHA-256 before validation: `954760af20a1b74275a9db50c99a09266da4f5d1e08f4b613c8a46f97adc9ce4`
- `Info.plist` SHA-256 before validation: `1fb62ba4d1aeaa5f66cde843217dd439ddb97154b1d6cdaea4340cf552c064fb`
- Both hashes were unchanged after validation, and `codesign --verify --deep --strict` passed.

## Renderer findings

- Settings Fast and the automatic-update row: `webview/assets/general-settings-BOz8t03P.js`
- Shared Fast allowance and configured-tier fallback: the `l46phxln-CXs7KrZ9.js` shared chunk
- Request-tier allowance: the `b0wqjrxp-CSNePJdQ.js` shared chunk
- `/fast` and Intelligence Speed: the `unq8yzli-givJ3wGc.js` shared chunk
- Automatic-update Settings target: existing `disable-automatic-updates-setting-26715-row-local-h`
- GPT-5.6: official application path; compatibility injection and selector widening are skipped
- Plugins: official application path; Plugins runtime targets and the legacy required initial label are skipped

Applying the version-filtered renderer patch rules to all 4,652 extracted
JavaScript assets changed five renderer modules: the shared Fast
allowance/fallback chunk, the Intelligence Speed and slash-command chunk, the
request-tier chunk, the Settings chunk, and one module carrying the desktop
settings schema copy. All changed JavaScript parsed successfully. Main-process
source-signature interception changed the three settings-schema copies and
`.vite/build/window-all-closed-CZr9g6FK.js`; manual update methods remain
intact.

## Validation boundary

- Direct installed-bundle inspection, version-filtered extracted-bundle patching, JavaScript parse checks, and regression coverage were performed.
- The installed `ChatGPT.app` signature was valid before and after validation, and no installed app files were written.
- ChatGPT was running during validation, so it was not forcibly quit and a fresh interactive runtime-launch smoke pass was not performed. Full launch and UI click-through remain separate manual checks from [`../real-app-validation.md`](../real-app-validation.md).
