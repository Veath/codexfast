# Codex.app 26.429.30905 Build 2345

## Summary

This note records the inspected bundle shape for the newly supported `Codex.app` build.

- Version: `26.429.30905`
- Build: `2345`
- Verification date: `2026-05-02`
- Result: `supported`

## Verified Feature Paths

- Composer `/fast` slash command
- Settings-side Fast control
- Composer `Intelligence` dropdown Speed submenu
- Plugins sidebar access for custom API users
- Plugins page content, plugin detail deep links, install-button availability, and install-modal content gates
- Official GPT-5.5 model-list visibility is expected through the app path used by `26.422.30944` and later builds

## Bundle Notes

- Assets are still under `app/webview/assets` inside `app.asar`.
- The inspected target files are `general-settings-DK9Gn4dx.js`, `composer-CNnjHdHK.js`, `index-DJATSIwz.js`, `skills-page-DowG2k1q.js`, `plugin-detail-page-BZVYeV3B.js`, and `use-plugin-install-flow-IA7dXDS4.js`.
- The Settings-side Fast gate still uses `de()` for availability and `Ve()` for the service-tier hook.
- The Fast slash command remains in `composer-CNnjHdHK.js` and still uses the existing `enabled:<local>` target shape.
- The composer `Intelligence` Speed submenu remains in `composer-CNnjHdHK.js` and still gates rendering through `qs(...)`. The adjacent selector call changed from the build `2312` fixture shape `zr(ER,n)` to `zr(DR,n)`.
- The Plugins sidebar disabled-nav state still uses `ms("533078438")`, `ed(authMethod)`, and the unified nav label expression `g({hostId:Or})&&!E`.
- The `/skills` route, plugin detail redirect, plugin install availability, and install modal content gates match the build `2312` shapes with renamed asset files.
- The model-list bridge still delegates to `model/list`, but `codexfast` skips GPT-5.5 apply targets on this build because GPT-5.5 is expected to be visible through the official app path from `26.422.30944` onward.

## Gate Notes

- The Settings-side Fast control still uses the guarded `if(!n)return null;` shape.
- The Fast slash command still uses `enabled:a`, where `a` is derived from `qs(...)`.
- The composer `Intelligence` Speed menu renders only when the local `qs(...)` result is truthy.
- The Plugins sidebar disabled-nav state uses `D=T&&E`.
- The unified Skills/Plugins nav label state uses `te=g({hostId:Or})&&!E`.
- The Plugins page content state uses `let m=f` after the API-key auth helper result.
- The plugin detail page redirect uses `if(qe(i))` after `authMethod` is read from `pe()`.
- The plugin install flow derives the top-level blocked reason from `h.length>0&&w===h.length?C?\`disabled-by-admin\`:\`connector-unavailable\`:null`; the patch keeps only the `disabled-by-admin` aggregate block.
- The plugin install modal content gate derives from the plugin detail apps and `ON_INSTALL` auth policy; the patch forces that local gate false so About, Includes, and Capabilities can render normally.

## Verification Notes

- Real installed-app status detection initially reported the build as unsupported before the whitelist update.
- Direct bundle inspection confirmed the supported feature paths are still present with renamed target filenames.
- The existing `26.429.20946` target specs matched all inspected targets except the composer `Intelligence` Speed gate because its `zr(...)` selector local changed. The matcher now accepts the minified selector local instead of hard-coding `ER`.
- Real installed-app apply patched eight targets, created `app.asar1`, updated the Electron ASAR integrity hash, re-signed successfully, and reset the Codex screen-recording permission record.
- Follow-up real installed-app status reported the Settings Fast, `/fast`, composer `Intelligence` Speed, Plugins sidebar, Plugins page content, plugin detail, plugin install availability, and plugin install modal content targets as enabled.
- Regression coverage includes the `26.429.30905` (`build 2345`) whitelist gate and apply/restore behavior for the Settings, Intelligence Speed, sidebar Plugins, Plugins page content, plugin detail, plugin install availability, and plugin install modal content target shapes.
