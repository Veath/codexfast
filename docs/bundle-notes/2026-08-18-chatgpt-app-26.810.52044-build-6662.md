# ChatGPT.app 26.810.52044 (build 6662)

- Strict compatibility key: `26.810.52044+6662`.
- Local bundle: `/Applications/ChatGPT.app`, bundle id `com.openai.codex`, executable `ChatGPT`.
- `webview/assets/app-initial-BCLYDefw.js` contains the shared Fast allowance, the new request helper shape `N2t(e,t,{priority:\`critical\`})`, configured-tier fallback, `/fast`, service-tier slash command, and renderer settings schema.
- `webview/assets/general-settings-yzMrcfmm.js` contains the Settings Fast option-count gate. The Intelligence Speed UI is driven from available options without the historical availability gate.
- The renderer bundle has no `disableAutomaticUpdates` string or Settings row. Main-process schema copies remain in `.vite/build/child-process-snapshot-worker.js`, `.vite/build/src-DY9Aq019.js`, and `.vite/build/worker.js`.
- The active updater is `.vite/build/window-all-closed-DqXEFZQJ.js`; source-signature interception still wraps background checks and forced automatic installs while preserving manual update methods.
- GPT-5.6 and Plugins use official application paths. Runtime launch skips their compatibility target families for this exact version/build.
- Validation is read-only against a temporary extraction of `app.asar`; the installed bundle, `Info.plist`, and signature were not modified.
