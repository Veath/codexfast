# ChatGPT.app 26.803.61601 (build 6396)

- Strict compatibility key: `26.803.61601+6396`.
- Local bundle: `/Applications/ChatGPT.app`, bundle id `com.openai.codex`.
- Renderer Fast, service-tier, `/fast`, Intelligence Speed, and renderer settings-schema targets moved to `webview/assets/app-initial-BYOVlUBL.js`; Settings Fast and the automatic-update row moved to `webview/assets/general-settings-Dz0zP8tf.js` without changing their guarded signatures.
- Main-process settings schema copies remain in `.vite/build/child-process-snapshot-worker.js`, `.vite/build/src-Cz_uUmVl.js`, and `.vite/build/worker.js`. Extracted-bundle validation exposed that the insertion-style guarded signature still matched after patching, so both renderer and main-process schema matchers now exclude an already inserted `disableAutomaticUpdates` field and remain idempotent.
- The active Sparkle updater remains `.vite/build/window-all-closed-9IR0zY5D.js`. Its nested `p` callback, `setAutomaticBackgroundDownloadsEnabledForMac`, and `setInterval(()=>void p(),e)` path still matches the callback-aware source-signature hook. Manual update methods remain available, and forced automatic install scheduling remains dynamically gated.
- GPT-5.6 and Plugins use official application paths; runtime launch skips their compatibility target families.

Applying the version-filtered patcher to an extracted copy changed only the expected renderer and main-process modules. A real runtime-launch pass reported the expected Fast and update targets. The installed app bundle was inspected read-only; `app.asar` and `Info.plist` hashes remained unchanged and the app signature remained valid.
