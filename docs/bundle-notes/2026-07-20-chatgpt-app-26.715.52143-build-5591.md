# ChatGPT.app 26.715.52143 build 5591

## Bundle metadata

- Installed app: `/Applications/ChatGPT.app`
- Bundle identifier: `com.openai.codex`
- `CFBundleShortVersionString`: `26.715.52143`
- `CFBundleVersion`: `5591`
- Strict compatibility key: `26.715.52143+5591`
- Extracted bundle used for validation: `/tmp/codexfast-5591.YJcbuM/app`
- `app.asar` SHA-256: `4dc2ca0aac6e4f6f858c504223bcdedf0b2d768fbc948d9f449f2da656f1b98f`
- `Info.plist` SHA-256: `f8d57032ec89349adcf38a9ce7236ed3b2e4f7285999ca00d19fabf6f5dd2640`

## Renderer findings

- Settings Fast and the automatic-update row: `webview/assets/general-settings-CsA3Lt9Z.js`
- Shared Fast allowance and configured-tier fallback: the `l46phxln` shared chunk
- Request-tier allowance: the `b0wqjrxp` shared chunk
- `/fast` and Intelligence Speed: the `unq8yzli` shared chunk
- Automatic-update Settings target: existing `disable-automatic-updates-setting-26715-row-local-h`
- GPT-5.6: official application path; compatibility injection and selector widening are skipped
- Plugins: official application path; Plugins runtime targets and the legacy required initial label are skipped

Applying the generated version-filtered renderer patcher to all 4,650 extracted
JavaScript files patched the complete Fast and automatic-update set without
writing extracted files. The target family is unchanged from build 5551; only
renderer asset names moved.

## Main-process updater findings

- The desktop settings schema continues to match the stable `preventSleepWhileRunning` source signature.
- The active Sparkle updater remains `.vite/build/window-all-closed-CZr9g6FK.js`.
- The existing callback-aware source-signature hook inserts the dynamic config check, preserves the production-appcast condition, guards forced automatic install scheduling, and keeps manual `checkForUpdates` and `installUpdatesIfAvailable` methods intact.
- Applying the settings and updater helpers across 21 `.vite/build/*.js` files changes four modules: three schema copies and the updater.

## Validation boundary

- Direct bundle inspection, regression coverage, generated CLI checks, a fresh real runtime-launch smoke pass, and installed-app hash/signature checks were performed.
- No installed `ChatGPT.app` files were modified.
- Runtime launch completed with the expected shared Fast, `/fast`, Intelligence Speed, and update-schema labels while official Plugins/GPT-5.6 targets remained filtered; the lazy Settings row was confirmed by extracted-bundle patching. Full UI click-through is still tracked separately by the manual checklist in [`../real-app-validation.md`](../real-app-validation.md).
