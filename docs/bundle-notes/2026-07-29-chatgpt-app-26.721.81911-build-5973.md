# ChatGPT.app 26.721.81911 Build 5973

## Metadata

- Bundle: `/Applications/ChatGPT.app`
- Bundle identifier: `com.openai.codex`
- `CFBundleShortVersionString`: `26.721.81911`
- `CFBundleVersion`: `5973`
- `app.asar` SHA-256 before adaptation: `3c9a101d9beec3718b0fcfc19e427c644a934045f48b3fe0e16b68b0b3f23e61`
- `Info.plist` SHA-256 before adaptation: `122c6af421c54739b5938ce30a155f8e128829860b4ba31d85d0d1ce04741f25`
- Deep strict code-signature validation passed before adaptation.

## Target Mapping

- `webview/assets/app-initial-CRKqnyc3.js`
  - Speed service-tier allowance
  - Speed service-tier request allowance
  - Speed service-tier conversation fallback
  - Fast slash command
  - Composer Intelligence Speed menu
  - Disable automatic updates schema
- `webview/assets/general-settings-CcPMYysK.js`
  - Speed setting
  - Disable automatic updates setting
- `.vite/build/child-process-snapshot-worker.js`, `.vite/build/src-BPbHdvxe.js`, and `.vite/build/worker.js`
  - Desktop settings schema copies discovered by source signature
- `.vite/build/window-all-closed-a9FCmS92.js`
  - Callback-aware automatic-update hook discovered by source signature

GPT-5.6 and Plugins use the official application paths for this repository's supported custom-API feature scope. The build therefore skips the legacy GPT-5.6 model-list/query-selector compatibility targets and all Plugins compatibility targets.

## Validation

- The unmodified `codexfast 0.64.0` launcher reproduced the expected fail-closed result because `26.721.81911+5973` was absent from the exact whitelist.
- Read-only ASAR extraction and target inspection found guarded signatures for every required Fast and automatic-update path.
- Applying the build-5848 version policy in memory changed five JavaScript modules and produced all eight required labels.
- Every changed renderer and main-process module parsed with zero JavaScript syntax diagnostics.
- The automatic-update hook preserved `checkForUpdates` and `installUpdatesIfAvailable`, so manual update actions remain available.
- Regression coverage verifies strict launch admission, absence of the legacy initial Plugins requirement, official Plugins filtering, official GPT-5.6 filtering, and generated CLI behavior.

## Validation Boundary

The installed ChatGPT application was running and hosts the active agent session, so it was not forcibly quit for a fresh interactive `codexfast launch` and UI click-through. Extracted-bundle validation, repository regressions, metadata hashes, and code-signature verification cover the non-interactive adaptation boundary. The manual checklist in `docs/real-app-validation.md` remains required before claiming a completed live UI smoke test.
