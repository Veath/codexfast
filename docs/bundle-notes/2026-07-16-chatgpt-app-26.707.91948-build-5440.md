# ChatGPT.app 26.707.91948 build 5440

## Bundle identity

- Installed app: `/Applications/ChatGPT.app`
- Bundle id: `com.openai.codex`
- `CFBundleShortVersionString`: `26.707.91948`
- `CFBundleVersion`: `5440`
- Strict compatibility key: `26.707.91948+5440`

## Compatibility conclusions

- Settings Fast remains guarded in `webview/assets/general-settings-DMO9G9gL.js`.
- The shared Fast allowance and configured-tier fallback remain guarded in `webview/assets/app-initial~app-main~quick-chat-window-page~chatgpt-conversation-page-Bv7yLYDT.js`.
- The request-tier helper remains guarded in the `jgoqfqy2` shared chunk.
- `/fast` and the Intelligence Speed gate remain guarded in the `iufn7mg3-DtuASjaM.js` shared chunk.
- Plugins remains supported by the official app path, so runtime launch skips every Plugins target and does not require the legacy `Plugins access` initial label.
- GPT-5.6 remains supported by the official app path because this build is newer than the `26.707.41301+5103` threshold.
- The automatic-update Settings row remains in `general-settings-DMO9G9gL.js`, but its final cached row local changed from `d` to `f`. The dedicated `disable-automatic-updates-setting-26707-row-local-f` target handles that exact shape while reusing the app-name-aware replacement.
- The active Sparkle manager remains `.vite/build/sqlite-B1YNeAip.js`. Existing source-signature discovery recognizes its background interval, automatic-download callback, and forced-install scheduler while preserving manual update actions.

## Validation boundary

- The installed `app.asar` was extracted to a temporary directory for read-only inspection.
- Regression coverage includes the strict whitelist, official Plugins/GPT-5.6 filtering, exact Settings syntax and idempotency, generated CLI behavior, and source-signature updater/schema patching.
- ChatGPT was active during the initial inspection and was not terminated. It later exited normally, allowing a fresh `node bin/codexfast launch` pass. Runtime launch detected `26.707.91948+5440`, reported supported compatibility, patched the required initial targets, and completed successfully.
- Automated Settings UI inspection was unavailable because the local Computer Use safety policy blocks `com.openai.codex`; the exact real Settings chunk was instead validated through direct patch application, JavaScript parsing, and second-pass byte equality.
- Installed `app.asar` and `Info.plist` hashes plus the application signature remained unchanged after the real launch.
